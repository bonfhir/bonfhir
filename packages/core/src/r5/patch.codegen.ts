/**
 * Typed patch builders for r5/5.0.0
 */

import {
  Account,
  AccountBalance,
  AccountCoverage,
  AccountDiagnosis,
  AccountGuarantor,
  AccountProcedure,
  AccountRelatedAccount,
  AccountStatus,
  ActivityDefinition,
  ActivityDefinitionDynamicValue,
  ActivityDefinitionParticipant,
  Address,
  AdministrableProductDefinition,
  AdministrableProductDefinitionProperty,
  AdministrableProductDefinitionRouteOfAdministration,
  AdministrativeGender,
  AdverseEventActuality,
  AdverseEventContributingFactor,
  AdverseEventMitigatingAction,
  AdverseEventParticipant,
  AdverseEventPreventiveAction,
  AdverseEventStatus,
  AdverseEventSupportingInfo,
  AdverseEventSuspectEntity,
  Age,
  AllFHIRTypes,
  AllLanguages,
  AllergyIntolerance,
  AllergyIntoleranceCategory,
  AllergyIntoleranceClinicalStatusCodes,
  AllergyIntoleranceCriticality,
  AllergyIntoleranceParticipant,
  AllergyIntoleranceReaction,
  AllergyIntoleranceVerificationStatus,
  Annotation,
  AnyDomainResourceType,
  Appointment,
  AppointmentParticipant,
  AppointmentRecurrenceTemplate,
  AppointmentResponse,
  AppointmentResponseStatus,
  AppointmentStatus,
  ArtifactAssessmentContent,
  ArtifactAssessmentDisposition,
  ArtifactAssessmentWorkflowStatus,
  Attachment,
  AuditEventAction,
  AuditEventAgent,
  AuditEventEntity,
  AuditEventOutcome,
  AuditEventSeverity,
  AuditEventSource,
  Availability,
  BiologicallyDerivedProduct,
  BiologicallyDerivedProductCollection,
  BiologicallyDerivedProductDispense,
  BiologicallyDerivedProductDispenseCodes,
  BiologicallyDerivedProductDispensePerformer,
  BiologicallyDerivedProductProperty,
  BodyStructure,
  BodyStructureIncludedStructure,
  BundleEntry,
  BundleLink,
  BundleType,
  CapabilityStatementDocument,
  CapabilityStatementImplementation,
  CapabilityStatementKind,
  CapabilityStatementMessaging,
  CapabilityStatementRest,
  CapabilityStatementSoftware,
  CarePlan,
  CarePlanActivity,
  CarePlanIntent,
  CareTeam,
  CareTeamParticipant,
  CareTeamStatus,
  ChargeItem,
  ChargeItemDefinitionApplicability,
  ChargeItemDefinitionPropertyGroup,
  ChargeItemPerformer,
  ChargeItemStatus,
  Citation,
  CitationCitedArtifact,
  CitationClassification,
  CitationStatusDate,
  CitationSummary,
  Claim,
  ClaimAccident,
  ClaimCareTeam,
  ClaimDiagnosis,
  ClaimEvent,
  ClaimInsurance,
  ClaimItem,
  ClaimPayee,
  ClaimProcedure,
  ClaimProcessingCodes,
  ClaimRelated,
  ClaimResponse,
  ClaimResponseAddItem,
  ClaimResponseError,
  ClaimResponseEvent,
  ClaimResponseInsurance,
  ClaimResponseItem,
  ClaimResponsePayment,
  ClaimResponseProcessNote,
  ClaimResponseTotal,
  ClaimSupportingInfo,
  ClinicalImpression,
  ClinicalImpressionFinding,
  ClinicalUseDefinition,
  ClinicalUseDefinitionContraindication,
  ClinicalUseDefinitionIndication,
  ClinicalUseDefinitionInteraction,
  ClinicalUseDefinitionType,
  ClinicalUseDefinitionUndesirableEffect,
  ClinicalUseDefinitionWarning,
  CodeSearchSupport,
  CodeSystemConcept,
  CodeSystemContentMode,
  CodeSystemFilter,
  CodeSystemHierarchyMeaning,
  CodeSystemProperty,
  CodeableConcept,
  CodeableReference,
  Coding,
  ColorCodesOrRGB,
  Communication,
  CommunicationPayload,
  CommunicationRequest,
  CommunicationRequestPayload,
  CompartmentDefinitionResource,
  CompartmentType,
  Composition,
  CompositionAttester,
  CompositionEvent,
  CompositionSection,
  CompositionStatus,
  ConceptMapAdditionalAttribute,
  ConceptMapGroup,
  ConceptMapProperty,
  Condition,
  ConditionClinicalStatusCodes,
  ConditionDefinitionMedication,
  ConditionDefinitionObservation,
  ConditionDefinitionPlan,
  ConditionDefinitionPrecondition,
  ConditionDefinitionQuestionnaire,
  ConditionParticipant,
  ConditionStage,
  ConditionVerificationStatus,
  Consent,
  ConsentPolicyBasis,
  ConsentProvision,
  ConsentProvisionType,
  ConsentState,
  ConsentVerification,
  ContactDetail,
  ContactPoint,
  Contract,
  ContractContentDefinition,
  ContractFriendly,
  ContractLegal,
  ContractResourceStatusCodes,
  ContractRule,
  ContractSigner,
  ContractTerm,
  Count,
  Coverage,
  CoverageClass,
  CoverageCostToBeneficiary,
  CoverageEligibilityRequest,
  CoverageEligibilityRequestEvent,
  CoverageEligibilityRequestInsurance,
  CoverageEligibilityRequestItem,
  CoverageEligibilityRequestSupportingInfo,
  CoverageEligibilityResponseError,
  CoverageEligibilityResponseEvent,
  CoverageEligibilityResponseInsurance,
  CoveragePaymentBy,
  Currencies,
  DataRequirement,
  DetectedIssueEvidence,
  DetectedIssueMitigation,
  DetectedIssueSeverity,
  DetectedIssueStatus,
  Device,
  DeviceAssociationOperation,
  DeviceAssociationStatusCodes,
  DeviceAssociationStatusReasonCodes,
  DeviceConformsTo,
  DeviceDefinition,
  DeviceDefinitionChargeItem,
  DeviceDefinitionClassification,
  DeviceDefinitionConformsTo,
  DeviceDefinitionCorrectiveAction,
  DeviceDefinitionDeviceName,
  DeviceDefinitionGuideline,
  DeviceDefinitionHasPart,
  DeviceDefinitionLink,
  DeviceDefinitionMaterial,
  DeviceDefinitionPackaging,
  DeviceDefinitionProperty,
  DeviceDefinitionRegulatoryIdentifier,
  DeviceDefinitionUdiDeviceIdentifier,
  DeviceDefinitionVersion,
  DeviceDispensePerformer,
  DeviceDispenseStatusCodes,
  DeviceMetric,
  DeviceMetricCalibration,
  DeviceMetricCategory,
  DeviceMetricOperationalStatus,
  DeviceName,
  DeviceProductionIdentifierInUDI,
  DeviceProperty,
  DeviceRequest,
  DeviceRequestParameter,
  DeviceUdiCarrier,
  DeviceUsageAdherence,
  DeviceUsageStatus,
  DeviceVersion,
  DiagnosticReport,
  DiagnosticReportMedia,
  DiagnosticReportStatus,
  DiagnosticReportSupportingInfo,
  DocumentReference,
  DocumentReferenceAttester,
  DocumentReferenceContent,
  DocumentReferenceRelatesTo,
  DocumentReferenceStatus,
  Dosage,
  Duration,
  EligibilityOutcome,
  EligibilityRequestPurpose,
  EligibilityResponsePurpose,
  Encounter,
  EncounterAdmission,
  EncounterDiagnosis,
  EncounterHistoryLocation,
  EncounterLocation,
  EncounterParticipant,
  EncounterReason,
  EncounterStatus,
  Endpoint,
  EndpointPayload,
  EndpointStatus,
  EnrollmentOutcome,
  EnrollmentRequest,
  EpisodeOfCare,
  EpisodeOfCareDiagnosis,
  EpisodeOfCareReason,
  EpisodeOfCareStatus,
  EpisodeOfCareStatusHistory,
  EventStatus,
  EvidenceCertainty,
  EvidenceReport,
  EvidenceReportRelatesTo,
  EvidenceReportSection,
  EvidenceReportSubject,
  EvidenceStatistic,
  EvidenceVariableCategory,
  EvidenceVariableCharacteristic,
  EvidenceVariableDefinition,
  EvidenceVariableHandling,
  ExampleScenarioActor,
  ExampleScenarioActorType,
  ExampleScenarioInstance,
  ExampleScenarioProcess,
  ExplanationOfBenefitAccident,
  ExplanationOfBenefitAddItem,
  ExplanationOfBenefitBenefitBalance,
  ExplanationOfBenefitCareTeam,
  ExplanationOfBenefitDiagnosis,
  ExplanationOfBenefitEvent,
  ExplanationOfBenefitInsurance,
  ExplanationOfBenefitItem,
  ExplanationOfBenefitPayee,
  ExplanationOfBenefitPayment,
  ExplanationOfBenefitProcedure,
  ExplanationOfBenefitProcessNote,
  ExplanationOfBenefitRelated,
  ExplanationOfBenefitStatus,
  ExplanationOfBenefitSupportingInfo,
  ExplanationOfBenefitTotal,
  ExtendedContactDetail,
  Extension,
  FHIRDeviceStatus,
  FHIRSubstanceStatus,
  FHIRVersion,
  FamilyHistoryStatus,
  FamilyMemberHistoryCondition,
  FamilyMemberHistoryParticipant,
  FamilyMemberHistoryProcedure,
  FinancialResourceStatusCodes,
  FlagStatus,
  FormularyItemStatusCodes,
  GenomicStudy,
  GenomicStudyAnalysis,
  GenomicStudyStatus,
  Goal,
  GoalLifecycleStatus,
  GoalTarget,
  GraphDefinitionLink,
  GraphDefinitionNode,
  Group,
  GroupCharacteristic,
  GroupMember,
  GroupMembershipBasis,
  GroupType,
  GuidanceResponseStatus,
  HealthcareService,
  HealthcareServiceEligibility,
  HumanName,
  Identifier,
  ImagingSelection,
  ImagingSelectionInstance,
  ImagingSelectionPerformer,
  ImagingSelectionStatus,
  ImagingStudy,
  ImagingStudySeries,
  ImagingStudyStatus,
  Immunization,
  ImmunizationEvaluationStatusCodes,
  ImmunizationPerformer,
  ImmunizationProgramEligibility,
  ImmunizationProtocolApplied,
  ImmunizationReaction,
  ImmunizationRecommendation,
  ImmunizationRecommendationRecommendation,
  ImmunizationStatusCodes,
  ImplementationGuideDefinition,
  ImplementationGuideDependsOn,
  ImplementationGuideGlobal,
  ImplementationGuideManifest,
  Ingredient,
  IngredientManufacturer,
  IngredientSubstance,
  InsurancePlan,
  InsurancePlanCoverage,
  InsurancePlanPlan,
  InventoryCountType,
  InventoryItemAssociation,
  InventoryItemCharacteristic,
  InventoryItemDescription,
  InventoryItemInstance,
  InventoryItemName,
  InventoryItemResponsibleOrganization,
  InventoryItemStatusCodes,
  InventoryReportInventoryListing,
  InventoryReportStatus,
  InvoiceLineItem,
  InvoiceParticipant,
  InvoiceStatus,
  Kind,
  LinkageItem,
  ListEntry,
  ListMode,
  ListStatus,
  Location,
  LocationMode,
  LocationPosition,
  LocationStatus,
  ManufacturedItemDefinition,
  ManufacturedItemDefinitionComponent,
  ManufacturedItemDefinitionProperty,
  MarketingStatus,
  MeasureGroup,
  MeasureImprovementNotation,
  MeasureReportGroup,
  MeasureReportStatus,
  MeasureReportType,
  MeasureSupplementalData,
  MeasureTerm,
  Medication,
  MedicationAdministration,
  MedicationAdministrationDosage,
  MedicationAdministrationPerformer,
  MedicationAdministrationStatusCodes,
  MedicationBatch,
  MedicationDispense,
  MedicationDispensePerformer,
  MedicationDispenseStatusCodes,
  MedicationDispenseSubstitution,
  MedicationIngredient,
  MedicationKnowledge,
  MedicationKnowledgeCost,
  MedicationKnowledgeDefinitional,
  MedicationKnowledgeIndicationGuideline,
  MedicationKnowledgeMedicineClassification,
  MedicationKnowledgeMonitoringProgram,
  MedicationKnowledgeMonograph,
  MedicationKnowledgePackaging,
  MedicationKnowledgeRegulatory,
  MedicationKnowledgeRelatedMedicationKnowledge,
  MedicationKnowledgeStatusCodes,
  MedicationKnowledgeStorageGuideline,
  MedicationRequest,
  MedicationRequestDispenseRequest,
  MedicationRequestIntent,
  MedicationRequestSubstitution,
  MedicationStatement,
  MedicationStatementAdherence,
  MedicationStatementStatusCodes,
  MedicationStatusCodes,
  MedicationrequestStatus,
  MedicinalProductDefinition,
  MedicinalProductDefinitionCharacteristic,
  MedicinalProductDefinitionContact,
  MedicinalProductDefinitionCrossReference,
  MedicinalProductDefinitionName,
  MedicinalProductDefinitionOperation,
  MessageDefinitionAllowedResponse,
  MessageDefinitionFocus,
  MessageHeaderDestination,
  MessageHeaderResponse,
  MessageHeaderSource,
  MessageSignificanceCategory,
  MessageheaderResponseRequest,
  MimeTypes,
  MolecularSequence,
  MolecularSequenceRelative,
  MonetaryComponent,
  Money,
  NamingSystemType,
  NamingSystemUniqueId,
  Narrative,
  NutritionIntake,
  NutritionIntakeConsumedItem,
  NutritionIntakeIngredientLabel,
  NutritionIntakePerformer,
  NutritionOrder,
  NutritionOrderEnteralFormula,
  NutritionOrderOralDiet,
  NutritionOrderSupplement,
  NutritionProduct,
  NutritionProductCharacteristic,
  NutritionProductIngredient,
  NutritionProductInstance,
  NutritionProductNutrient,
  NutritionProductStatus,
  Observation,
  ObservationComponent,
  ObservationDataType,
  ObservationDefinition,
  ObservationDefinitionComponent,
  ObservationDefinitionQualifiedValue,
  ObservationReferenceRange,
  ObservationStatus,
  ObservationTriggeredBy,
  OperationDefinitionOverload,
  OperationDefinitionParameter,
  OperationKind,
  OperationOutcome,
  OperationOutcomeIssue,
  Organization,
  OrganizationQualification,
  PackagedProductDefinition,
  PackagedProductDefinitionLegalStatusOfSupply,
  PackagedProductDefinitionPackaging,
  ParameterDefinition,
  Parameters,
  ParametersParameter,
  Patient,
  PatientCommunication,
  PatientContact,
  PatientLink,
  PaymentOutcome,
  PaymentReconciliation,
  PaymentReconciliationAllocation,
  PaymentReconciliationProcessNote,
  Period,
  PermissionJustification,
  PermissionRule,
  PermissionRuleCombining,
  PermissionStatus,
  PersonCommunication,
  PersonLink,
  PlanDefinition,
  PlanDefinitionAction,
  PlanDefinitionActor,
  PlanDefinitionGoal,
  Practitioner,
  PractitionerCommunication,
  PractitionerQualification,
  PractitionerRole,
  Procedure,
  ProcedureFocalDevice,
  ProcedurePerformer,
  ProductShelfLife,
  Provenance,
  ProvenanceAgent,
  ProvenanceEntity,
  PublicationStatus,
  Quantity,
  Questionnaire,
  QuestionnaireItem,
  QuestionnaireResponse,
  QuestionnaireResponseItem,
  QuestionnaireResponseStatus,
  Range,
  Ratio,
  Reference,
  RegulatedAuthorizationCase,
  RelatedArtifact,
  RelatedPerson,
  RelatedPersonCommunication,
  RequestIntent,
  RequestOrchestration,
  RequestOrchestrationAction,
  RequestPriority,
  RequestResourceTypes,
  RequestStatus,
  RequirementsStatement,
  ResearchStudy,
  ResearchStudyAssociatedParty,
  ResearchStudyComparisonGroup,
  ResearchStudyLabel,
  ResearchStudyObjective,
  ResearchStudyOutcomeMeasure,
  ResearchStudyProgressStatus,
  ResearchStudyRecruitment,
  ResearchSubject,
  ResearchSubjectProgress,
  Resource,
  ResourceType,
  RiskAssessment,
  RiskAssessmentPrediction,
  SPDXLicense,
  SampledData,
  Schedule,
  SearchComparator,
  SearchModifierCode,
  SearchParamType,
  SearchParameterComponent,
  SearchProcessingModeType,
  SequenceType,
  ServiceRequest,
  ServiceRequestOrderDetail,
  ServiceRequestPatientInstruction,
  Signature,
  Slot,
  SlotStatus,
  Specimen,
  SpecimenCollection,
  SpecimenCombined,
  SpecimenContainer,
  SpecimenDefinition,
  SpecimenDefinitionTypeTested,
  SpecimenFeature,
  SpecimenProcessing,
  SpecimenStatus,
  StructureDefinitionContext,
  StructureDefinitionDifferential,
  StructureDefinitionKind,
  StructureDefinitionMapping,
  StructureDefinitionSnapshot,
  StructureMapConst,
  StructureMapGroup,
  StructureMapStructure,
  SubmitDataUpdateType,
  Subscription,
  SubscriptionFilterBy,
  SubscriptionNotificationType,
  SubscriptionParameter,
  SubscriptionPayloadContent,
  SubscriptionStatusCodes,
  SubscriptionStatusNotificationEvent,
  SubscriptionTopicCanFilterBy,
  SubscriptionTopicEventTrigger,
  SubscriptionTopicNotificationShape,
  SubscriptionTopicResourceTrigger,
  Substance,
  SubstanceDefinition,
  SubstanceDefinitionCharacterization,
  SubstanceDefinitionCode,
  SubstanceDefinitionMoiety,
  SubstanceDefinitionMolecularWeight,
  SubstanceDefinitionName,
  SubstanceDefinitionProperty,
  SubstanceDefinitionRelationship,
  SubstanceDefinitionSourceMaterial,
  SubstanceDefinitionStructure,
  SubstanceIngredient,
  SubstanceNucleicAcid,
  SubstanceNucleicAcidSubunit,
  SubstancePolymer,
  SubstancePolymerMonomerSet,
  SubstancePolymerRepeat,
  SubstanceProtein,
  SubstanceProteinSubunit,
  SubstanceReferenceInformation,
  SubstanceReferenceInformationGene,
  SubstanceReferenceInformationGeneElement,
  SubstanceReferenceInformationTarget,
  SubstanceSourceMaterialFractionDescription,
  SubstanceSourceMaterialOrganism,
  SubstanceSourceMaterialPartDescription,
  SupplyDelivery,
  SupplyDeliveryStatus,
  SupplyDeliverySuppliedItem,
  SupplyDeliverySupplyItemType,
  SupplyRequest,
  SupplyRequestParameter,
  SupplyRequestStatus,
  Task,
  TaskInput,
  TaskIntent,
  TaskOutput,
  TaskPerformer,
  TaskRestriction,
  TaskStatus,
  TerminologyCapabilitiesClosure,
  TerminologyCapabilitiesCodeSystem,
  TerminologyCapabilitiesExpansion,
  TerminologyCapabilitiesImplementation,
  TerminologyCapabilitiesSoftware,
  TerminologyCapabilitiesTranslation,
  TerminologyCapabilitiesValidateCode,
  TestPlanDependency,
  TestPlanTestCase,
  TestReportParticipant,
  TestReportResult,
  TestReportSetup,
  TestReportStatus,
  TestReportTeardown,
  TestReportTest,
  TestScriptDestination,
  TestScriptFixture,
  TestScriptMetadata,
  TestScriptOrigin,
  TestScriptScope,
  TestScriptSetup,
  TestScriptTeardown,
  TestScriptTest,
  TestScriptVariable,
  Timing,
  Transport,
  TransportInput,
  TransportIntent,
  TransportOutput,
  TransportRestriction,
  TransportStatus,
  TriggerDefinition,
  TypeDerivationRule,
  UsageContext,
  Use,
  ValueSetCompose,
  ValueSetExpansion,
  ValueSetScope,
  VerificationResultAttestation,
  VerificationResultPrimarySource,
  VerificationResultStatus,
  VerificationResultValidator,
  VersionIndependentResourceTypesAll,
  VirtualServiceDetail,
  VisionPrescription,
  VisionPrescriptionLensSpecification,
} from "./fhir-types.codegen";
import { FhirPatchBuilder } from "./patch";

/**
 * Return a typed patch builder for a specific resource.
 */
export function fhirPatch(resourceType?: null | undefined): FhirPatchBuilder;
export function fhirPatch<
  TDomainResourceType extends AnyDomainResourceType = AnyDomainResourceType
>(resourceType: TDomainResourceType): ExtractPatchBuilder<TDomainResourceType>;
export function fhirPatch<
  TDomainResourceType extends AnyDomainResourceType = AnyDomainResourceType
>(
  resourceType?: TDomainResourceType | null | undefined
): FhirPatchBuilder | ExtractPatchBuilder<TDomainResourceType>;
export function fhirPatch<
  TDomainResourceType extends AnyDomainResourceType = AnyDomainResourceType
>(
  resourceType?: TDomainResourceType | null | undefined
): FhirPatchBuilder | ExtractPatchBuilder<TDomainResourceType> {
  if (!resourceType) {
    return new FhirPatchBuilder();
  }

  return new FhirPatchBuilder() as unknown as ExtractPatchBuilder<TDomainResourceType>;
}

export interface ResourceFhirPatchBuilder {
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/implicitRules", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/language", value: AllLanguages): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DomainResourceFhirPatchBuilder
  extends ResourceFhirPatchBuilder {
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contained, inline Resources
   */
  add(path: "/contained", value: Array<Resource>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contained/${number | "-"}`, value: Resource): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional content defined by implementations
   */
  add(path: "/extension", value: Array<Extension>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/extension/${number | "-"}`, value: Extension): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Extensions that cannot be ignored
   */
  add(path: "/modifierExtension", value: Array<Extension>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/modifierExtension/${number | "-"}`, value: Extension): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/text", value: Narrative): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface AccountFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Account";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Calculated account balance(s)
   */
  add(path: "/balance", value: Array<AccountBalance>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/balance/${number | "-"}`, value: AccountBalance): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/billingStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/calculatedAt", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The party(s) that are responsible for covering the payment of this account, and what order should they be applied to the account
   */
  add(path: "/coverage", value: Array<AccountCoverage>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/coverage/${number | "-"}`, value: AccountCoverage): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/currency", value: Currencies): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The list of diagnoses relevant to this account
   */
  add(path: "/diagnosis", value: Array<AccountDiagnosis>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/diagnosis/${number | "-"}`, value: AccountDiagnosis): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The parties ultimately responsible for balancing the Account
   */
  add(path: "/guarantor", value: Array<AccountGuarantor>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/guarantor/${number | "-"}`, value: AccountGuarantor): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Account number
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/owner", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The list of procedures relevant to this account
   */
  add(path: "/procedure", value: Array<AccountProcedure>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/procedure/${number | "-"}`, value: AccountProcedure): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Other associated accounts related to this account
   */
  add(path: "/relatedAccount", value: Array<AccountRelatedAccount>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relatedAccount/${number | "-"}`,
    value: AccountRelatedAccount
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/servicePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: AccountStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The entity that caused the expenses
   */
  add(
    path: "/subject",
    value: Array<
      Reference<
        | Patient
        | Device
        | Practitioner
        | PractitionerRole
        | Location
        | HealthcareService
        | Organization
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/subject/${number | "-"}`,
    value: Reference<
      | Patient
      | Device
      | Practitioner
      | PractitionerRole
      | Location
      | HealthcareService
      | Organization
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ActivityDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ActivityDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/asNeededBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/asNeededCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the content
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What part of body to perform on
   */
  add(path: "/bodySite", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/bodySite/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doNotPerform", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Detailed dosage instructions
   */
  add(path: "/dosage", value: Array<Dosage>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/dosage/${number | "-"}`, value: Dosage): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Dynamic aspects of the definition
   */
  add(
    path: "/dynamicValue",
    value: Array<ActivityDefinitionDynamicValue>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/dynamicValue/${number | "-"}`,
    value: ActivityDefinitionDynamicValue
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the content
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the content
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the activity definition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: RequestIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for activity definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/kind", value: RequestResourceTypes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Logic used by the activity definition
   */
  add(path: "/library", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/library/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What observations are required to perform this action
   */
  add(path: "/observationRequirement", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/observationRequirement/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What observations must be produced by this action
   */
  add(path: "/observationResultRequirement", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/observationResultRequirement/${number | "-"}`,
    value: string
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who should participate in the action
   */
  add(path: "/participant", value: Array<ActivityDefinitionParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/participant/${number | "-"}`,
    value: ActivityDefinitionParticipant
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/productReference",
    value: Reference<Medication | Ingredient | Substance | SubstanceDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/productCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/profile", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the content
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What specimens are required to perform this action
   */
  add(path: "/specimenRequirement", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specimenRequirement/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subjectReference",
    value: Reference<
      | Group
      | MedicinalProductDefinition
      | SubstanceDefinition
      | AdministrableProductDefinition
      | ManufacturedItemDefinition
      | PackagedProductDefinition
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subtitle", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timingTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timingAge", value: Age): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timingRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timingDuration", value: Duration): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/transform", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usage", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ActorDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ActorDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/capabilities", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Definition of this actor in another context / IG
   */
  add(path: "/derivedFrom", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFrom/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/documentation", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the actor definition (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for actor definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reference to more information about the actor
   */
  add(path: "/reference", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reference/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: ExampleScenarioActorType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface AdministrableProductDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "AdministrableProductDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/administrableDoseForm", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/device", value: Reference<DeviceDefinition>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * References a product from which one or more of the constituent parts of that product can be prepared and used as described by this administrable product
   */
  add(
    path: "/formOf",
    value: Array<Reference<MedicinalProductDefinition>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/formOf/${number | "-"}`,
    value: Reference<MedicinalProductDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An identifier for the administrable product
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The ingredients of this administrable medicinal product. This is only needed if the ingredients are not specified either using ManufacturedItemDefiniton, or using by incoming references from the Ingredient resource
   */
  add(path: "/ingredient", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/ingredient/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Indicates the specific manufactured items that are part of the 'formOf' product that are used in the preparation of this specific administrable form
   */
  add(
    path: "/producedFrom",
    value: Array<Reference<ManufacturedItemDefinition>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/producedFrom/${number | "-"}`,
    value: Reference<ManufacturedItemDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Characteristics e.g. a product's onset of action
   */
  add(
    path: "/property",
    value: Array<AdministrableProductDefinitionProperty>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/property/${number | "-"}`,
    value: AdministrableProductDefinitionProperty
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The path by which the product is taken into or makes contact with the body
   */
  add(
    path: "/routeOfAdministration",
    value: Array<AdministrableProductDefinitionRouteOfAdministration>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/routeOfAdministration/${number | "-"}`,
    value: AdministrableProductDefinitionRouteOfAdministration
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/unitOfPresentation", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface AdverseEventFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "AdverseEvent";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/actuality", value: AdverseEventActuality): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * wrong-patient | procedure-mishap | medication-mishap | device | unsafe-physical-environment | hospital-aquired-infection | wrong-body-site
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contributing factors suspected to have increased the probability or severity of the adverse event
   */
  add(
    path: "/contributingFactor",
    value: Array<AdverseEventContributingFactor>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/contributingFactor/${number | "-"}`,
    value: AdverseEventContributingFactor
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/detected", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expectedInResearchStudy", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for the event
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Ameliorating actions taken after the adverse event occured in order to reduce the extent of harm
   */
  add(
    path: "/mitigatingAction",
    value: Array<AdverseEventMitigatingAction>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/mitigatingAction/${number | "-"}`,
    value: AdverseEventMitigatingAction
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comment on adverse event
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of outcome from the adverse event
   */
  add(path: "/outcome", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/outcome/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who was involved in the adverse event or the potential adverse event and what they did
   */
  add(path: "/participant", value: Array<AdverseEventParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/participant/${number | "-"}`,
    value: AdverseEventParticipant
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Preventive actions that contributed to avoiding the adverse event
   */
  add(
    path: "/preventiveAction",
    value: Array<AdverseEventPreventiveAction>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/preventiveAction/${number | "-"}`,
    value: AdverseEventPreventiveAction
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recordedDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/recorder",
    value: Reference<
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
      | ResearchSubject
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Effect on the subject due to this event
   */
  add(
    path: "/resultingEffect",
    value: Array<Reference<Condition | Observation>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/resultingEffect/${number | "-"}`,
    value: Reference<Condition | Observation>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/seriousness", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: AdverseEventStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Research study that the subject is enrolled in
   */
  add(path: "/study", value: Array<Reference<ResearchStudy>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/study/${number | "-"}`, value: Reference<ResearchStudy>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      Patient | Group | Practitioner | RelatedPerson | ResearchSubject
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supporting information relevant to the event
   */
  add(path: "/supportingInfo", value: Array<AdverseEventSupportingInfo>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: AdverseEventSupportingInfo
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The suspected agent causing the adverse event
   */
  add(path: "/suspectEntity", value: Array<AdverseEventSuspectEntity>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/suspectEntity/${number | "-"}`,
    value: AdverseEventSuspectEntity
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface AllergyIntoleranceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "AllergyIntolerance";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * food | medication | environment | biologic
   */
  add(path: "/category", value: Array<AllergyIntoleranceCategory>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/category/${number | "-"}`,
    value: AllergyIntoleranceCategory
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/clinicalStatus",
    value: AllergyIntoleranceClinicalStatusCodes
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/criticality", value: AllergyIntoleranceCriticality): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External ids for this item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastOccurrence", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional text not captured in other fields
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetAge", value: Age): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who or what participated in the activities related to the allergy or intolerance and how they were involved
   */
  add(path: "/participant", value: Array<AllergyIntoleranceParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/participant/${number | "-"}`,
    value: AllergyIntoleranceParticipant
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Adverse Reaction Events linked to exposure to substance
   */
  add(path: "/reaction", value: Array<AllergyIntoleranceReaction>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/reaction/${number | "-"}`,
    value: AllergyIntoleranceReaction
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recordedDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/verificationStatus",
    value: AllergyIntoleranceVerificationStatus
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface AppointmentFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Appointment";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The set of accounts that may be used for billing for this Appointment
   */
  add(path: "/account", value: Array<Reference<Account>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/account/${number | "-"}`, value: Reference<Account>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/appointmentType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The request this appointment is allocated to assess
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<CarePlan | DeviceRequest | MedicationRequest | ServiceRequest>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      CarePlan | DeviceRequest | MedicationRequest | ServiceRequest
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/cancellationDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/cancellationReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classification when becoming an encounter
   */
  add(path: "/class", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/class/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/end", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Ids for this item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/minutesDuration", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional comments
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceChanged", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/originatingAppointment", value: Reference<Appointment>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Participants involved in appointment
   */
  add(path: "/participant", value: Array<AppointmentParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/participant/${number | "-"}`,
    value: AppointmentParticipant
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Detailed information and instructions for the patient
   */
  add(path: "/patientInstruction", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/patientInstruction/${number | "-"}`,
    value: CodeableReference
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/previousAppointment", value: Reference<Appointment>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason this appointment is scheduled
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recurrenceId", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Details of the recurrence pattern/template used to generate occurrences
   */
  add(
    path: "/recurrenceTemplate",
    value: Array<AppointmentRecurrenceTemplate>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/recurrenceTemplate/${number | "-"}`,
    value: AppointmentRecurrenceTemplate
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Appointment replaced by this Appointment
   */
  add(path: "/replaces", value: Array<Reference<Appointment>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/replaces/${number | "-"}`, value: Reference<Appointment>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Potential date/time interval(s) requested to allocate the appointment within
   */
  add(path: "/requestedPeriod", value: Array<Period>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/requestedPeriod/${number | "-"}`, value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A broad categorization of the service that is to be performed during this appointment
   */
  add(path: "/serviceCategory", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/serviceCategory/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The specific service that is to be performed during this appointment
   */
  add(path: "/serviceType", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/serviceType/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The slots that this appointment is filling
   */
  add(path: "/slot", value: Array<Reference<Slot>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/slot/${number | "-"}`, value: Reference<Slot>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The specialty of a practitioner that would be required to perform the service requested in this appointment
   */
  add(path: "/specialty", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specialty/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/start", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: AppointmentStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information to support the appointment
   */
  add(path: "/supportingInformation", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInformation/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Connection details of a virtual service (e.g. conference call)
   */
  add(path: "/virtualService", value: Array<VirtualServiceDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/virtualService/${number | "-"}`,
    value: VirtualServiceDetail
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface AppointmentResponseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "AppointmentResponse";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/actor",
    value: Reference<
      | Patient
      | Group
      | Practitioner
      | PractitionerRole
      | RelatedPerson
      | Device
      | HealthcareService
      | Location
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/appointment", value: Reference<Appointment>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/comment", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/end", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Ids for this item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/participantStatus", value: AppointmentResponseStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Role of participant in the appointment
   */
  add(path: "/participantType", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/participantType/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/proposedNewTime", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recurrenceId", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recurring", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/start", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ArtifactAssessmentFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ArtifactAssessment";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/artifactReference", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/artifactCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/artifactUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/citeAsReference", value: Reference<Citation>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/citeAsMarkdown", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comment, classifier, or rating content
   */
  add(path: "/content", value: Array<ArtifactAssessmentContent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/content/${number | "-"}`, value: ArtifactAssessmentContent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/disposition", value: ArtifactAssessmentDisposition): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the artifact assessment
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/workflowStatus", value: ArtifactAssessmentWorkflowStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface AuditEventFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "AuditEvent";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/action", value: AuditEventAction): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Actor involved in the event
   */
  add(path: "/agent", value: Array<AuditEventAgent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/agent/${number | "-"}`, value: AuditEventAgent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Authorization related to the event
   */
  add(path: "/authorization", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/authorization/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Workflow authorization within which this event occurred
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        | CarePlan
        | DeviceRequest
        | ImmunizationRecommendation
        | MedicationRequest
        | NutritionOrder
        | ServiceRequest
        | Task
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      | CarePlan
      | DeviceRequest
      | ImmunizationRecommendation
      | MedicationRequest
      | NutritionOrder
      | ServiceRequest
      | Task
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type/identifier of event
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Data or objects used
   */
  add(path: "/entity", value: Array<AuditEventEntity>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/entity/${number | "-"}`, value: AuditEventEntity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurredPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurredDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outcome", value: AuditEventOutcome): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recorded", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/severity", value: AuditEventSeverity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/source", value: AuditEventSource): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface BasicFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Basic";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/author",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Patient
      | RelatedPerson
      | Organization
      | Device
      | CareTeam
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Resource>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface BinaryFhirPatchBuilder extends ResourceFhirPatchBuilder {
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/contentType", value: MimeTypes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/data", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/securityContext", value: Reference<Resource>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface BiologicallyDerivedProductFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "BiologicallyDerivedProduct";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/biologicalSourceEvent", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/collection", value: BiologicallyDerivedProductCollection): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/division", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expirationDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instance identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The parent biologically-derived product
   */
  add(
    path: "/parent",
    value: Array<Reference<BiologicallyDerivedProduct>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/parent/${number | "-"}`,
    value: Reference<BiologicallyDerivedProduct>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Processing facilities responsible for the labeling and distribution of this biologically derived product
   */
  add(path: "/processingFacility", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/processingFacility/${number | "-"}`,
    value: Reference<Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/productCategory", value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/productCode", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/productStatus", value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A property that is specific to this BiologicallyDerviedProduct instance
   */
  add(
    path: "/property",
    value: Array<BiologicallyDerivedProductProperty>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/property/${number | "-"}`,
    value: BiologicallyDerivedProductProperty
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request to obtain and/or infuse this product
   */
  add(path: "/request", value: Array<Reference<ServiceRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/request/${number | "-"}`, value: Reference<ServiceRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/storageTempRequirements", value: Range): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface BiologicallyDerivedProductDispenseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "BiologicallyDerivedProductDispense";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The order or request that this dispense is fulfilling
   */
  add(path: "/basedOn", value: Array<Reference<ServiceRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<ServiceRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/destination", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for this dispense
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/matchStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional notes
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/originRelationshipType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Short description
   */
  add(
    path: "/partOf",
    value: Array<Reference<BiologicallyDerivedProductDispense>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<BiologicallyDerivedProductDispense>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Indicates who or what performed an action
   */
  add(
    path: "/performer",
    value: Array<BiologicallyDerivedProductDispensePerformer>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: BiologicallyDerivedProductDispensePerformer
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/preparedDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/product", value: Reference<BiologicallyDerivedProduct>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: BiologicallyDerivedProductDispenseCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usageInstruction", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/whenHandedOver", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface BodyStructureFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "BodyStructure";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Bodystructure identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Attached images
   */
  add(path: "/image", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/image/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Included anatomic location(s)
   */
  add(
    path: "/includedStructure",
    value: Array<BodyStructureIncludedStructure>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/includedStructure/${number | "-"}`,
    value: BodyStructureIncludedStructure
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/morphology", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface BundleFhirPatchBuilder extends ResourceFhirPatchBuilder {
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Entry in the bundle - will have a resource or information
   */
  add(path: "/entry", value: Array<BundleEntry>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/entry/${number | "-"}`, value: BundleEntry): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/identifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/issues", value: Resource): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Links related to this Bundle
   */
  add(path: "/link", value: Array<BundleLink>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/link/${number | "-"}`, value: BundleLink): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/signature", value: Signature): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timestamp", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/total", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: BundleType): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CanonicalResourceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CanonicalResource";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the {{title}}
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for {{title}} (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CapabilityStatementFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CapabilityStatement";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Languages supported
   */
  add(path: "/acceptLanguage", value: Array<AllLanguages>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/acceptLanguage/${number | "-"}`, value: AllLanguages): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Document definition
   */
  add(path: "/document", value: Array<CapabilityStatementDocument>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/document/${number | "-"}`,
    value: CapabilityStatementDocument
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/fhirVersion", value: FHIRVersion): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * formats supported (xml | json | ttl | mime type)
   */
  add(path: "/format", value: Array<MimeTypes>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/format/${number | "-"}`, value: MimeTypes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the CapabilityStatement (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/implementation", value: CapabilityStatementImplementation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Implementation guides supported
   */
  add(path: "/implementationGuide", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/implementationGuide/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Canonical URL of another capability statement this adds to
   */
  add(path: "/imports", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/imports/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Canonical URL of another capability statement this implements
   */
  add(path: "/instantiates", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiates/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for capability statement (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/kind", value: CapabilityStatementKind): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * If messaging is supported
   */
  add(path: "/messaging", value: Array<CapabilityStatementMessaging>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/messaging/${number | "-"}`,
    value: CapabilityStatementMessaging
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patch formats supported
   */
  add(path: "/patchFormat", value: Array<MimeTypes>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/patchFormat/${number | "-"}`, value: MimeTypes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * If the endpoint is a RESTful one
   */
  add(path: "/rest", value: Array<CapabilityStatementRest>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/rest/${number | "-"}`, value: CapabilityStatementRest): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/software", value: CapabilityStatementSoftware): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CarePlanFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CarePlan";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Action to occur or has occurred as part of plan
   */
  add(path: "/activity", value: Array<CarePlanActivity>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/activity/${number | "-"}`, value: CarePlanActivity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Health issues this plan addresses
   */
  add(path: "/addresses", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/addresses/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Fulfills plan, proposal or order
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        CarePlan | ServiceRequest | RequestOrchestration | NutritionOrder
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      CarePlan | ServiceRequest | RequestOrchestration | NutritionOrder
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who's involved in plan?
   */
  add(path: "/careTeam", value: Array<Reference<CareTeam>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/careTeam/${number | "-"}`, value: Reference<CareTeam>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of plan
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who provided the content of the care plan
   */
  add(
    path: "/contributor",
    value: Array<
      Reference<
        | Patient
        | Practitioner
        | PractitionerRole
        | Device
        | RelatedPerson
        | Organization
        | CareTeam
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/contributor/${number | "-"}`,
    value: Reference<
      | Patient
      | Practitioner
      | PractitionerRole
      | Device
      | RelatedPerson
      | Organization
      | CareTeam
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/custodian",
    value: Reference<
      | Patient
      | Practitioner
      | PractitionerRole
      | Device
      | RelatedPerson
      | Organization
      | CareTeam
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Desired outcome of plan
   */
  add(path: "/goal", value: Array<Reference<Goal>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/goal/${number | "-"}`, value: Reference<Goal>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Ids for this plan
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: CarePlanIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments about the plan
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced CarePlan
   */
  add(path: "/partOf", value: Array<Reference<CarePlan>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: Reference<CarePlan>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * CarePlan replaced by this CarePlan
   */
  add(path: "/replaces", value: Array<Reference<CarePlan>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/replaces/${number | "-"}`, value: Reference<CarePlan>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: RequestStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information considered as part of plan
   */
  add(path: "/supportingInfo", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CareTeamFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CareTeam";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of team
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Ids for this team
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Organization responsible for the care team
   */
  add(
    path: "/managingOrganization",
    value: Array<Reference<Organization>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/managingOrganization/${number | "-"}`,
    value: Reference<Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the CareTeam
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Members of the team
   */
  add(path: "/participant", value: Array<CareTeamParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/participant/${number | "-"}`, value: CareTeamParticipant): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why the care team exists
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: CareTeamStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A contact detail for the care team (that applies to all members)
   */
  add(path: "/telecom", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/telecom/${number | "-"}`, value: ContactPoint): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ChargeItemFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ChargeItem";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Account to place this charge
   */
  add(path: "/account", value: Array<Reference<Account>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/account/${number | "-"}`, value: Reference<Account>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Anatomical location, if relevant
   */
  add(path: "/bodysite", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/bodysite/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/costCenter", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Resource defining the code of this ChargeItem
   */
  add(path: "/definitionCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/definitionCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Defining information about the code of this charge item
   */
  add(path: "/definitionUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/definitionUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/enteredDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/enterer",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | Patient
      | Device
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the ChargeItem
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/overrideReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced ChargeItem
   */
  add(path: "/partOf", value: Array<Reference<ChargeItem>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: Reference<ChargeItem>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who performed charged service
   */
  add(path: "/performer", value: Array<ChargeItemPerformer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/performer/${number | "-"}`, value: ChargeItemPerformer): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/performingOrganization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Product charged
   */
  add(path: "/product", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/product/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why was the charged  service rendered?
   */
  add(path: "/reason", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/requestingOrganization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Which rendered service is being charged?
   */
  add(path: "/service", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/service/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ChargeItemStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Further information supporting this charge
   */
  add(path: "/supportingInformation", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInformation/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/totalPriceComponent", value: MonetaryComponent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/unitPriceComponent", value: MonetaryComponent): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ChargeItemDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ChargeItemDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Whether or not the billing code is applicable
   */
  add(
    path: "/applicability",
    value: Array<ChargeItemDefinitionApplicability>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/applicability/${number | "-"}`,
    value: ChargeItemDefinitionApplicability
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Underlying externally-defined charge item definition
   */
  add(path: "/derivedFromUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFromUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the charge item definition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instances this definition applies to
   */
  add(
    path: "/instance",
    value: Array<
      Reference<
        | Medication
        | Substance
        | Device
        | DeviceDefinition
        | ActivityDefinition
        | PlanDefinition
        | HealthcareService
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/instance/${number | "-"}`,
    value: Reference<
      | Medication
      | Substance
      | Device
      | DeviceDefinition
      | ActivityDefinition
      | PlanDefinition
      | HealthcareService
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for charge item definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A larger definition of which this particular definition is a component or step
   */
  add(path: "/partOf", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Group of properties which are applicable under the same conditions
   */
  add(
    path: "/propertyGroup",
    value: Array<ChargeItemDefinitionPropertyGroup>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/propertyGroup/${number | "-"}`,
    value: ChargeItemDefinitionPropertyGroup
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Completed or terminated request(s) whose function is taken by this new request
   */
  add(path: "/replaces", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/replaces/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CitationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Citation";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the citation record
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/citedArtifact", value: CitationCitedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The assignment to an organizing scheme
   */
  add(path: "/classification", value: Array<CitationClassification>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/classification/${number | "-"}`,
    value: CitationClassification
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher of the citation record
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The status of the citation record
   */
  add(path: "/currentState", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/currentState/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the citation record
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the citation record
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifier for the citation record itself
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for citation record (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Used for general notes and annotations not coded elsewhere
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Artifact related to the citation record
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the citation record
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An effective date or period for a status of the citation record
   */
  add(path: "/statusDate", value: Array<CitationStatusDate>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/statusDate/${number | "-"}`, value: CitationStatusDate): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A human-readable display of key concepts to represent the citation
   */
  add(path: "/summary", value: Array<CitationSummary>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/summary/${number | "-"}`, value: CitationSummary): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the citation record content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ClaimFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Claim";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/accident", value: ClaimAccident): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/billablePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Members of the care team
   */
  add(path: "/careTeam", value: Array<ClaimCareTeam>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/careTeam/${number | "-"}`, value: ClaimCareTeam): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Pertinent diagnosis information
   */
  add(path: "/diagnosis", value: Array<ClaimDiagnosis>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/diagnosis/${number | "-"}`, value: ClaimDiagnosis): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/diagnosisRelatedGroup", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Encounters associated with the listed treatments
   */
  add(path: "/encounter", value: Array<Reference<Encounter>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/encounter/${number | "-"}`, value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/enterer",
    value: Reference<Practitioner | PractitionerRole | Patient | RelatedPerson>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Event information
   */
  add(path: "/event", value: Array<ClaimEvent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/event/${number | "-"}`, value: ClaimEvent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/facility", value: Reference<Location | Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/fundsReserve", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for claim
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient insurance information
   */
  add(path: "/insurance", value: Array<ClaimInsurance>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/insurance/${number | "-"}`, value: ClaimInsurance): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/insurer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Product or service provided
   */
  add(path: "/item", value: Array<ClaimItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/item/${number | "-"}`, value: ClaimItem): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/originalPrescription",
    value: Reference<DeviceRequest | MedicationRequest | VisionPrescription>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patientPaid", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/payee", value: ClaimPayee): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/prescription",
    value: Reference<DeviceRequest | MedicationRequest | VisionPrescription>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Clinical procedures performed
   */
  add(path: "/procedure", value: Array<ClaimProcedure>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/procedure/${number | "-"}`, value: ClaimProcedure): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/provider",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/referral", value: Reference<ServiceRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Prior or corollary claims
   */
  add(path: "/related", value: Array<ClaimRelated>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/related/${number | "-"}`, value: ClaimRelated): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supporting information
   */
  add(path: "/supportingInfo", value: Array<ClaimSupportingInfo>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: ClaimSupportingInfo
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/total", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Number for tracking
   */
  add(path: "/traceNumber", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/traceNumber/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/use", value: Use): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ClaimResponseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ClaimResponse";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Insurer added line items
   */
  add(path: "/addItem", value: Array<ClaimResponseAddItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/addItem/${number | "-"}`, value: ClaimResponseAddItem): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request for additional information
   */
  add(
    path: "/communicationRequest",
    value: Array<Reference<CommunicationRequest>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/communicationRequest/${number | "-"}`,
    value: Reference<CommunicationRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/decision", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/diagnosisRelatedGroup", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/disposition", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Encounters associated with the listed treatments
   */
  add(path: "/encounter", value: Array<Reference<Encounter>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/encounter/${number | "-"}`, value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Processing errors
   */
  add(path: "/error", value: Array<ClaimResponseError>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/error/${number | "-"}`, value: ClaimResponseError): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Event information
   */
  add(path: "/event", value: Array<ClaimResponseEvent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/event/${number | "-"}`, value: ClaimResponseEvent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/form", value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/formCode", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/fundsReserve", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for a claim response
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient insurance information
   */
  add(path: "/insurance", value: Array<ClaimResponseInsurance>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/insurance/${number | "-"}`, value: ClaimResponseInsurance): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/insurer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Adjudication for claim line items
   */
  add(path: "/item", value: Array<ClaimResponseItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/item/${number | "-"}`, value: ClaimResponseItem): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outcome", value: ClaimProcessingCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/payeeType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/payment", value: ClaimResponsePayment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/preAuthPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/preAuthRef", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Note concerning adjudication
   */
  add(path: "/processNote", value: Array<ClaimResponseProcessNote>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/processNote/${number | "-"}`,
    value: ClaimResponseProcessNote
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/request", value: Reference<Claim>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requestor",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Adjudication totals
   */
  add(path: "/total", value: Array<ClaimResponseTotal>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/total/${number | "-"}`, value: ClaimResponseTotal): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Number for tracking
   */
  add(path: "/traceNumber", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/traceNumber/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/use", value: Use): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ClinicalImpressionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ClinicalImpression";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/changePattern", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectiveDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Possible or likely findings and diagnoses
   */
  add(path: "/finding", value: Array<ClinicalImpressionFinding>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/finding/${number | "-"}`, value: ClinicalImpressionFinding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the ClinicalImpression
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/performer",
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/previous", value: Reference<ClinicalImpression>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Relevant impressions of patient state
   */
  add(
    path: "/problem",
    value: Array<Reference<Condition | AllergyIntolerance>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/problem/${number | "-"}`,
    value: Reference<Condition | AllergyIntolerance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Estimate of likely outcome
   */
  add(path: "/prognosisCodeableConcept", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/prognosisCodeableConcept/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * RiskAssessment expressing likely outcome
   */
  add(
    path: "/prognosisReference",
    value: Array<Reference<RiskAssessment>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/prognosisReference/${number | "-"}`,
    value: Reference<RiskAssessment>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Clinical Protocol followed
   */
  add(path: "/protocol", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/protocol/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: EventStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/summary", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information supporting the clinical impression
   */
  add(path: "/supportingInfo", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: Reference<Resource>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ClinicalUseDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ClinicalUseDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A categorisation of the issue, primarily for dividing warnings into subject heading areas such as "Pregnancy", "Overdose"
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/contraindication",
    value: ClinicalUseDefinitionContraindication
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for this issue
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/indication", value: ClinicalUseDefinitionIndication): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/interaction", value: ClinicalUseDefinitionInteraction): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Logic used by the clinical use definition
   */
  add(path: "/library", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/library/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The population group to which this applies
   */
  add(path: "/population", value: Array<Reference<Group>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/population/${number | "-"}`, value: Reference<Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The medication, product, substance, device, procedure etc. for which this is an indication
   */
  add(
    path: "/subject",
    value: Array<
      Reference<
        | MedicinalProductDefinition
        | Medication
        | ActivityDefinition
        | PlanDefinition
        | Device
        | DeviceDefinition
        | Substance
        | NutritionProduct
        | BiologicallyDerivedProduct
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/subject/${number | "-"}`,
    value: Reference<
      | MedicinalProductDefinition
      | Medication
      | ActivityDefinition
      | PlanDefinition
      | Device
      | DeviceDefinition
      | Substance
      | NutritionProduct
      | BiologicallyDerivedProduct
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: ClinicalUseDefinitionType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/undesirableEffect",
    value: ClinicalUseDefinitionUndesirableEffect
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/warning", value: ClinicalUseDefinitionWarning): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CodeSystemFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CodeSystem";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the CodeSystem
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/caseSensitive", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/compositional", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Concepts in the code system
   */
  add(path: "/concept", value: Array<CodeSystemConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/concept/${number | "-"}`, value: CodeSystemConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/content", value: CodeSystemContentMode): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/count", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the CodeSystem
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the CodeSystem
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Filter that can be used in a value set
   */
  add(path: "/filter", value: Array<CodeSystemFilter>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/filter/${number | "-"}`, value: CodeSystemFilter): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/hierarchyMeaning", value: CodeSystemHierarchyMeaning): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the code system (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for code system (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information supplied about each concept
   */
  add(path: "/property", value: Array<CodeSystemProperty>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/property/${number | "-"}`, value: CodeSystemProperty): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the CodeSystem
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/supplements", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueSet", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionNeeded", value: boolean): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CommunicationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Communication";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Resources that pertain to this communication
   */
  add(path: "/about", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/about/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request fulfilled by this communication
   */
  add(path: "/basedOn", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Message category
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reply to
   */
  add(path: "/inResponseTo", value: Array<Reference<Communication>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/inResponseTo/${number | "-"}`,
    value: Reference<Communication>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A channel of communication
   */
  add(path: "/medium", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/medium/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the communication
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event (e.g. Communication, Procedure)
   */
  add(path: "/partOf", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Message payload
   */
  add(path: "/payload", value: Array<CommunicationPayload>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/payload/${number | "-"}`, value: CommunicationPayload): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Indication for message
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/received", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who the information is shared with
   */
  add(
    path: "/recipient",
    value: Array<
      Reference<
        | CareTeam
        | Device
        | Group
        | HealthcareService
        | Location
        | Organization
        | Patient
        | Practitioner
        | PractitionerRole
        | RelatedPerson
        | Endpoint
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/recipient/${number | "-"}`,
    value: Reference<
      | CareTeam
      | Device
      | Group
      | HealthcareService
      | Location
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
      | Endpoint
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/sender",
    value: Reference<
      | Device
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
      | HealthcareService
      | Endpoint
      | CareTeam
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sent", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: EventStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/topic", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CommunicationRequestFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CommunicationRequest";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Resources that pertain to this communication request
   */
  add(path: "/about", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/about/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authoredOn", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Fulfills plan or proposal
   */
  add(path: "/basedOn", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Message category
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doNotPerform", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/groupIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who should share the information
   */
  add(
    path: "/informationProvider",
    value: Array<
      Reference<
        | Device
        | Organization
        | Patient
        | Practitioner
        | PractitionerRole
        | RelatedPerson
        | HealthcareService
        | Endpoint
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/informationProvider/${number | "-"}`,
    value: Reference<
      | Device
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
      | HealthcareService
      | Endpoint
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: RequestIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A channel of communication
   */
  add(path: "/medium", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/medium/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about communication request
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Message payload
   */
  add(path: "/payload", value: Array<CommunicationRequestPayload>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/payload/${number | "-"}`,
    value: CommunicationRequestPayload
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why is communication needed?
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who to share the information with
   */
  add(
    path: "/recipient",
    value: Array<
      Reference<
        | Device
        | Organization
        | Patient
        | Practitioner
        | PractitionerRole
        | RelatedPerson
        | Group
        | CareTeam
        | HealthcareService
        | Endpoint
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/recipient/${number | "-"}`,
    value: Reference<
      | Device
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
      | Group
      | CareTeam
      | HealthcareService
      | Endpoint
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request(s) replaced by this request
   */
  add(path: "/replaces", value: Array<Reference<CommunicationRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/replaces/${number | "-"}`,
    value: Reference<CommunicationRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requester",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | Patient
      | RelatedPerson
      | Device
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: RequestStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CompartmentDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CompartmentDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CompartmentType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * How a resource is related to the compartment
   */
  add(path: "/resource", value: Array<CompartmentDefinitionResource>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/resource/${number | "-"}`,
    value: CompartmentDefinitionResource
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/search", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CompositionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Composition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Attests to accuracy of composition
   */
  add(path: "/attester", value: Array<CompositionAttester>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/attester/${number | "-"}`, value: CompositionAttester): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who and/or what authored the composition
   */
  add(
    path: "/author",
    value: Array<
      Reference<
        | Practitioner
        | PractitionerRole
        | Device
        | Patient
        | RelatedPerson
        | Organization
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/author/${number | "-"}`,
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Device
      | Patient
      | RelatedPerson
      | Organization
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Categorization of Composition
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/custodian", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The clinical service(s) being documented
   */
  add(path: "/event", value: Array<CompositionEvent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/event/${number | "-"}`, value: CompositionEvent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Version-independent identifier for the Composition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * For any additional notes
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Relationships to other compositions/documents
   */
  add(path: "/relatesTo", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatesTo/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Composition is broken into sections
   */
  add(path: "/section", value: Array<CompositionSection>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/section/${number | "-"}`, value: CompositionSection): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: CompositionStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who and/or what the composition is about
   */
  add(path: "/subject", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subject/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ConceptMapFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ConceptMap";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Definition of an additional attribute to act as a data source or target
   */
  add(
    path: "/additionalAttribute",
    value: Array<ConceptMapAdditionalAttribute>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/additionalAttribute/${number | "-"}`,
    value: ConceptMapAdditionalAttribute
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the ConceptMap
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the ConceptMap
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the ConceptMap
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Same source and target systems
   */
  add(path: "/group", value: Array<ConceptMapGroup>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/group/${number | "-"}`, value: ConceptMapGroup): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the concept map
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for concept map (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional properties of the mapping
   */
  add(path: "/property", value: Array<ConceptMapProperty>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/property/${number | "-"}`, value: ConceptMapProperty): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the ConceptMap
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sourceScopeUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sourceScopeCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/targetScopeUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/targetScopeCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ConditionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Condition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/abatementDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/abatementAge", value: Age): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/abatementPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/abatementRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/abatementString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Anatomical location, if relevant
   */
  add(path: "/bodySite", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/bodySite/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * problem-list-item | encounter-diagnosis
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/clinicalStatus", value: ConditionClinicalStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supporting evidence for the verification status
   */
  add(path: "/evidence", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/evidence/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Ids for this condition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information about the Condition
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetAge", value: Age): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/onsetString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who or what participated in the activities related to the condition and how they were involved
   */
  add(path: "/participant", value: Array<ConditionParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/participant/${number | "-"}`, value: ConditionParticipant): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recordedDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/severity", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Stage/grade, usually assessed formally
   */
  add(path: "/stage", value: Array<ConditionStage>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/stage/${number | "-"}`, value: ConditionStage): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/verificationStatus", value: ConditionVerificationStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ConditionDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ConditionDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bodySite", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Formal Definition for the condition
   */
  add(path: "/definition", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/definition/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/hasBodySite", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/hasSeverity", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/hasStage", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the condition definition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for condition definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Medications particularly relevant for this condition
   */
  add(path: "/medication", value: Array<ConditionDefinitionMedication>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/medication/${number | "-"}`,
    value: ConditionDefinitionMedication
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Observations particularly relevant to this condition
   */
  add(path: "/observation", value: Array<ConditionDefinitionObservation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/observation/${number | "-"}`,
    value: ConditionDefinitionObservation
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Plan that is appropriate
   */
  add(path: "/plan", value: Array<ConditionDefinitionPlan>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/plan/${number | "-"}`, value: ConditionDefinitionPlan): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Observation that suggets this condition
   */
  add(
    path: "/precondition",
    value: Array<ConditionDefinitionPrecondition>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/precondition/${number | "-"}`,
    value: ConditionDefinitionPrecondition
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Questionnaire for this condition
   */
  add(
    path: "/questionnaire",
    value: Array<ConditionDefinitionQuestionnaire>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/questionnaire/${number | "-"}`,
    value: ConditionDefinitionQuestionnaire
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/severity", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/stage", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subtitle", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Appropriate team for this condition
   */
  add(path: "/team", value: Array<Reference<CareTeam>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/team/${number | "-"}`, value: Reference<CareTeam>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ConsentFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Consent";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classification of the consent statement - for indexing/retrieval
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Consent Enforcer
   */
  add(
    path: "/controller",
    value: Array<
      Reference<HealthcareService | Organization | Patient | Practitioner>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/controller/${number | "-"}`,
    value: Reference<HealthcareService | Organization | Patient | Practitioner>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/decision", value: ConsentProvisionType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who is agreeing to the policy and rules
   */
  add(
    path: "/grantee",
    value: Array<
      Reference<
        | CareTeam
        | HealthcareService
        | Organization
        | Patient
        | Practitioner
        | RelatedPerson
        | PractitionerRole
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/grantee/${number | "-"}`,
    value: Reference<
      | CareTeam
      | HealthcareService
      | Organization
      | Patient
      | Practitioner
      | RelatedPerson
      | PractitionerRole
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who is granting rights according to the policy and rules
   */
  add(
    path: "/grantor",
    value: Array<
      Reference<
        | CareTeam
        | HealthcareService
        | Organization
        | Patient
        | Practitioner
        | RelatedPerson
        | PractitionerRole
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/grantor/${number | "-"}`,
    value: Reference<
      | CareTeam
      | HealthcareService
      | Organization
      | Patient
      | Practitioner
      | RelatedPerson
      | PractitionerRole
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifier for this record (external references)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Consent workflow management
   */
  add(
    path: "/manager",
    value: Array<
      Reference<HealthcareService | Organization | Patient | Practitioner>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/manager/${number | "-"}`,
    value: Reference<HealthcareService | Organization | Patient | Practitioner>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/policyBasis", value: ConsentPolicyBasis): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Human Readable Policy
   */
  add(path: "/policyText", value: Array<Reference<DocumentReference>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/policyText/${number | "-"}`,
    value: Reference<DocumentReference>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Constraints to the base Consent.policyRule/Consent.policy
   */
  add(path: "/provision", value: Array<ConsentProvision>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/provision/${number | "-"}`, value: ConsentProvision): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Regulations establishing base Consent
   */
  add(path: "/regulatoryBasis", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/regulatoryBasis/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Source from which this consent is taken
   */
  add(path: "/sourceAttachment", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/sourceAttachment/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Source from which this consent is taken
   */
  add(
    path: "/sourceReference",
    value: Array<
      Reference<Consent | DocumentReference | Contract | QuestionnaireResponse>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/sourceReference/${number | "-"}`,
    value: Reference<
      Consent | DocumentReference | Contract | QuestionnaireResponse
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ConsentState): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Practitioner | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Consent Verified by patient or family
   */
  add(path: "/verification", value: Array<ConsentVerification>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/verification/${number | "-"}`, value: ConsentVerification): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ContractFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Contract";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Acronym or short name
   */
  add(path: "/alias", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/alias/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/applies", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/author",
    value: Reference<Patient | Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Authority under which this Contract has standing
   */
  add(path: "/authority", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/authority/${number | "-"}`, value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/contentDefinition", value: ContractContentDefinition): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/contentDerivative", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A sphere of control governed by an authoritative jurisdiction, organization, or person
   */
  add(path: "/domain", value: Array<Reference<Location>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/domain/${number | "-"}`, value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expirationType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contract Friendly Language
   */
  add(path: "/friendly", value: Array<ContractFriendly>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/friendly/${number | "-"}`, value: ContractFriendly): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contract number
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesCanonical", value: Reference<Contract>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/issued", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contract Legal Language
   */
  add(path: "/legal", value: Array<ContractLegal>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/legal/${number | "-"}`, value: ContractLegal): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/legallyBindingAttachment", value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/legallyBindingReference",
    value: Reference<
      Composition | DocumentReference | QuestionnaireResponse | Contract
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/legalState", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Key event in Contract History
   */
  add(path: "/relevantHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relevantHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Computable Contract Language
   */
  add(path: "/rule", value: Array<ContractRule>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/rule/${number | "-"}`, value: ContractRule): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/scope", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contract Signatory
   */
  add(path: "/signer", value: Array<ContractSigner>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/signer/${number | "-"}`, value: ContractSigner): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific Location
   */
  add(path: "/site", value: Array<Reference<Location>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/site/${number | "-"}`, value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ContractResourceStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contract Target Entity
   */
  add(path: "/subject", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subject/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subtitle", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Subtype within the context of type
   */
  add(path: "/subType", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subType/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Extra Information
   */
  add(path: "/supportingInfo", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contract Term List
   */
  add(path: "/term", value: Array<ContractTerm>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/term/${number | "-"}`, value: ContractTerm): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/topicCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/topicReference", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CoverageFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Coverage";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/beneficiary", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional coverage classifications
   */
  add(path: "/class", value: Array<CoverageClass>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/class/${number | "-"}`, value: CoverageClass): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contract details
   */
  add(path: "/contract", value: Array<Reference<Contract>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contract/${number | "-"}`, value: Reference<Contract>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient payments for services/products
   */
  add(
    path: "/costToBeneficiary",
    value: Array<CoverageCostToBeneficiary>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/costToBeneficiary/${number | "-"}`,
    value: CoverageCostToBeneficiary
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dependent", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier(s) for this coverage
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/insurancePlan", value: Reference<InsurancePlan>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/insurer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/kind", value: Kind): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/network", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/order", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Self-pay parties and responsibility
   */
  add(path: "/paymentBy", value: Array<CoveragePaymentBy>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/paymentBy/${number | "-"}`, value: CoveragePaymentBy): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/policyHolder",
    value: Reference<Patient | RelatedPerson | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/relationship", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subrogation", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subscriber", value: Reference<Patient | RelatedPerson>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * ID assigned to the subscriber
   */
  add(path: "/subscriberId", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subscriberId/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CoverageEligibilityRequestFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CoverageEligibilityRequest";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/enterer",
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Event information
   */
  add(path: "/event", value: Array<CoverageEligibilityRequestEvent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/event/${number | "-"}`,
    value: CoverageEligibilityRequestEvent
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/facility", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for coverage eligiblity request
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient insurance information
   */
  add(
    path: "/insurance",
    value: Array<CoverageEligibilityRequestInsurance>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/insurance/${number | "-"}`,
    value: CoverageEligibilityRequestInsurance
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/insurer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Item to be evaluated for eligibiity
   */
  add(path: "/item", value: Array<CoverageEligibilityRequestItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/item/${number | "-"}`,
    value: CoverageEligibilityRequestItem
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/provider",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * auth-requirements | benefits | discovery | validation
   */
  add(path: "/purpose", value: Array<EligibilityRequestPurpose>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/purpose/${number | "-"}`, value: EligibilityRequestPurpose): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/servicedDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/servicedPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supporting information
   */
  add(
    path: "/supportingInfo",
    value: Array<CoverageEligibilityRequestSupportingInfo>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: CoverageEligibilityRequestSupportingInfo
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface CoverageEligibilityResponseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "CoverageEligibilityResponse";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/disposition", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Processing errors
   */
  add(path: "/error", value: Array<CoverageEligibilityResponseError>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/error/${number | "-"}`,
    value: CoverageEligibilityResponseError
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Event information
   */
  add(path: "/event", value: Array<CoverageEligibilityResponseEvent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/event/${number | "-"}`,
    value: CoverageEligibilityResponseEvent
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/form", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for coverage eligiblity request
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient insurance information
   */
  add(
    path: "/insurance",
    value: Array<CoverageEligibilityResponseInsurance>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/insurance/${number | "-"}`,
    value: CoverageEligibilityResponseInsurance
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/insurer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outcome", value: EligibilityOutcome): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/preAuthRef", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * auth-requirements | benefits | discovery | validation
   */
  add(path: "/purpose", value: Array<EligibilityResponsePurpose>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/purpose/${number | "-"}`,
    value: EligibilityResponsePurpose
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/request", value: Reference<CoverageEligibilityRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requestor",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/servicedDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/servicedPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DetectedIssueFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DetectedIssue";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/author",
    value: Reference<
      Patient | RelatedPerson | Practitioner | PractitionerRole | Device
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of detected issue, e.g. drug-drug, duplicate therapy, etc
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/detail", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supporting evidence
   */
  add(path: "/evidence", value: Array<DetectedIssueEvidence>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/evidence/${number | "-"}`, value: DetectedIssueEvidence): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/identifiedDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/identifiedPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique id for the detected issue
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Problem resource
   */
  add(path: "/implicated", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/implicated/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Step taken to address
   */
  add(path: "/mitigation", value: Array<DetectedIssueMitigation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/mitigation/${number | "-"}`,
    value: DetectedIssueMitigation
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reference", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/severity", value: DetectedIssueSeverity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: DetectedIssueStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | Group
      | Device
      | Location
      | Organization
      | Procedure
      | Practitioner
      | Medication
      | Substance
      | BiologicallyDerivedProduct
      | NutritionProduct
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DeviceFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Device";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/availabilityStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/biologicalSourceEvent", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Indicates a high-level grouping of the device
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifies the standards, specifications, or formal guidances for the capabilities supported by the device
   */
  add(path: "/conformsTo", value: Array<DeviceConformsTo>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/conformsTo/${number | "-"}`, value: DeviceConformsTo): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Details for human/organization for support
   */
  add(path: "/contact", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactPoint): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/cycle", value: Count): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/definition", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/displayName", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/duration", value: Duration): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Technical endpoints providing access to electronic services provided by the device
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expirationDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Linked device acting as a communication/data collector, translator or controller
   */
  add(path: "/gateway", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/gateway/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instance identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lotNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/manufactureDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/manufacturer", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/mode", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/modelNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The name or names of the device as known to the manufacturer and/or patient
   */
  add(path: "/name", value: Array<DeviceName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/name/${number | "-"}`, value: DeviceName): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Device notes and comments
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/owner", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/parent", value: Reference<Device>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/partNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Inherent, essentially fixed, characteristics of the device.  e.g., time properties, size, material, etc.
   */
  add(path: "/property", value: Array<DeviceProperty>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/property/${number | "-"}`, value: DeviceProperty): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Safety Characteristics of Device
   */
  add(path: "/safety", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/safety/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/serialNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FHIRDeviceStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The kind or type of device
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique Device Identifier (UDI) Barcode string
   */
  add(path: "/udiCarrier", value: Array<DeviceUdiCarrier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/udiCarrier/${number | "-"}`, value: DeviceUdiCarrier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The actual design of the device or software version running on the device
   */
  add(path: "/version", value: Array<DeviceVersion>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/version/${number | "-"}`, value: DeviceVersion): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DeviceAssociationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DeviceAssociation";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bodyStructure", value: Reference<BodyStructure>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Describes the relationship between the device and subject
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/device", value: Reference<Device>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instance identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The details about the device when it is in use to describe its operation
   */
  add(path: "/operation", value: Array<DeviceAssociationOperation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/operation/${number | "-"}`,
    value: DeviceAssociationOperation
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: DeviceAssociationStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The reasons given for the current association status
   */
  add(
    path: "/statusReason",
    value: Array<DeviceAssociationStatusReasonCodes>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/statusReason/${number | "-"}`,
    value: DeviceAssociationStatusReasonCodes
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<Patient | Group | Practitioner | RelatedPerson | Device>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DeviceDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DeviceDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Billing code or reference associated with the device
   */
  add(path: "/chargeItem", value: Array<DeviceDefinitionChargeItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/chargeItem/${number | "-"}`,
    value: DeviceDefinitionChargeItem
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What kind of device or device system this is
   */
  add(
    path: "/classification",
    value: Array<DeviceDefinitionClassification>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/classification/${number | "-"}`,
    value: DeviceDefinitionClassification
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifies the standards, specifications, or formal guidances for the capabilities supported by the device
   */
  add(path: "/conformsTo", value: Array<DeviceDefinitionConformsTo>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/conformsTo/${number | "-"}`,
    value: DeviceDefinitionConformsTo
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Details for human/organization for support
   */
  add(path: "/contact", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactPoint): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/correctiveAction", value: DeviceDefinitionCorrectiveAction): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The name or names of the device as given by the manufacturer
   */
  add(path: "/deviceName", value: Array<DeviceDefinitionDeviceName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/deviceName/${number | "-"}`,
    value: DeviceDefinitionDeviceName
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/guideline", value: DeviceDefinitionGuideline): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A device, part of the current one
   */
  add(path: "/hasPart", value: Array<DeviceDefinitionHasPart>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/hasPart/${number | "-"}`, value: DeviceDefinitionHasPart): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instance identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Language code for the human-readable text strings produced by the device (all supported)
   */
  add(path: "/languageCode", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/languageCode/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An associated device, attached to, used with, communicating with or linking a previous or new device model to the focal device
   */
  add(path: "/link", value: Array<DeviceDefinitionLink>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/link/${number | "-"}`, value: DeviceDefinitionLink): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/manufacturer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A substance used to create the material(s) of which the device is made
   */
  add(path: "/material", value: Array<DeviceDefinitionMaterial>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/material/${number | "-"}`, value: DeviceDefinitionMaterial): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/modelNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Device notes and comments
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/owner", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information about the packaging of the device, i.e. how the device is packaged
   */
  add(path: "/packaging", value: Array<DeviceDefinitionPackaging>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/packaging/${number | "-"}`,
    value: DeviceDefinitionPackaging
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/partNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * lot-number | manufactured-date | serial-number | expiration-date | biological-source | software-version
   */
  add(
    path: "/productionIdentifierInUDI",
    value: Array<DeviceProductionIdentifierInUDI>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/productionIdentifierInUDI/${number | "-"}`,
    value: DeviceProductionIdentifierInUDI
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Inherent, essentially fixed, characteristics of this kind of device, e.g., time properties, size, etc
   */
  add(path: "/property", value: Array<DeviceDefinitionProperty>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/property/${number | "-"}`, value: DeviceDefinitionProperty): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Regulatory identifier(s) associated with this device
   */
  add(
    path: "/regulatoryIdentifier",
    value: Array<DeviceDefinitionRegulatoryIdentifier>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/regulatoryIdentifier/${number | "-"}`,
    value: DeviceDefinitionRegulatoryIdentifier
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Safety characteristics of the device
   */
  add(path: "/safety", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/safety/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Shelf Life and storage information
   */
  add(path: "/shelfLifeStorage", value: Array<ProductShelfLife>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/shelfLifeStorage/${number | "-"}`, value: ProductShelfLife): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique Device Identifier (UDI) Barcode string
   */
  add(
    path: "/udiDeviceIdentifier",
    value: Array<DeviceDefinitionUdiDeviceIdentifier>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/udiDeviceIdentifier/${number | "-"}`,
    value: DeviceDefinitionUdiDeviceIdentifier
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The version of the device or software
   */
  add(path: "/version", value: Array<DeviceDefinitionVersion>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/version/${number | "-"}`, value: DeviceDefinitionVersion): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DeviceDispenseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DeviceDispense";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The order or request that this dispense is fulfilling
   */
  add(
    path: "/basedOn",
    value: Array<Reference<CarePlan | DeviceRequest>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<CarePlan | DeviceRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of device dispense
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/destination", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/device", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A list of relevant lifecycle events
   */
  add(path: "/eventHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/eventHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for this dispensation
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information about the dispense
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The bigger event that this dispense is a part of
   */
  add(path: "/partOf", value: Array<Reference<Procedure>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: Reference<Procedure>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who performed event
   */
  add(path: "/performer", value: Array<DeviceDispensePerformer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/performer/${number | "-"}`, value: DeviceDispensePerformer): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/preparedDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/receiver",
    value: Reference<
      Patient | Practitioner | RelatedPerson | Location | PractitionerRole
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: DeviceDispenseStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Practitioner>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information that supports the dispensing of the device
   */
  add(path: "/supportingInformation", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInformation/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usageInstruction", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/whenHandedOver", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DeviceMetricFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DeviceMetric";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Describes the calibrations that have been performed or that are required to be performed
   */
  add(path: "/calibration", value: Array<DeviceMetricCalibration>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/calibration/${number | "-"}`,
    value: DeviceMetricCalibration
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/category", value: DeviceMetricCategory): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/color", value: ColorCodesOrRGB): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/device", value: Reference<Device>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instance identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/measurementFrequency", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/operationalStatus", value: DeviceMetricOperationalStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/unit", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DeviceRequestFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DeviceRequest";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/asNeeded", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/asNeededFor", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authoredOn", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What request fulfills
   */
  add(path: "/basedOn", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doNotPerform", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/groupIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Request identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Associated insurance coverage
   */
  add(
    path: "/insurance",
    value: Array<Reference<Coverage | ClaimResponse>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/insurance/${number | "-"}`,
    value: Reference<Coverage | ClaimResponse>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: RequestIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Notes or comments
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Device details
   */
  add(path: "/parameter", value: Array<DeviceRequestParameter>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parameter/${number | "-"}`, value: DeviceRequestParameter): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/performer", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Coded/Linked Reason for request
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request provenance
   */
  add(path: "/relevantHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relevantHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What request replaces
   */
  add(path: "/replaces", value: Array<Reference<DeviceRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/replaces/${number | "-"}`, value: Reference<DeviceRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requester",
    value: Reference<Device | Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: RequestStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<Patient | Group | Location | Device>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional clinical information
   */
  add(path: "/supportingInfo", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: Reference<Resource>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DeviceUsageFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DeviceUsage";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/adherence", value: DeviceUsageAdherence): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Fulfills plan, proposal or order
   */
  add(path: "/basedOn", value: Array<Reference<ServiceRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<ServiceRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bodySite", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The category of the statement - classifying how the statement is made
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/context", value: Reference<Encounter | EpisodeOfCare>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dateAsserted", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supporting information
   */
  add(
    path: "/derivedFrom",
    value: Array<
      Reference<
        | ServiceRequest
        | Procedure
        | Claim
        | Observation
        | QuestionnaireResponse
        | DocumentReference
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/derivedFrom/${number | "-"}`,
    value: Reference<
      | ServiceRequest
      | Procedure
      | Claim
      | Observation
      | QuestionnaireResponse
      | DocumentReference
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/device", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External identifier for this record
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/informationSource",
    value: Reference<
      Patient | Practitioner | PractitionerRole | RelatedPerson | Organization
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Addition details (comments, instructions)
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why device was used
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: DeviceUsageStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timingTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timingPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timingDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The reason for asserting the usage status - for example forgot, lost, stolen, broken
   */
  add(path: "/usageReason", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/usageReason/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usageStatus", value: DeviceUsageStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DiagnosticReportFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DiagnosticReport";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What was requested
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        | CarePlan
        | ImmunizationRecommendation
        | MedicationRequest
        | NutritionOrder
        | ServiceRequest
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      | CarePlan
      | ImmunizationRecommendation
      | MedicationRequest
      | NutritionOrder
      | ServiceRequest
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Service category
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/composition", value: Reference<Composition>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/conclusion", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Codes for the clinical conclusion of test results
   */
  add(path: "/conclusionCode", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/conclusionCode/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectiveDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for report
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/issued", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Key images or data associated with this report
   */
  add(path: "/media", value: Array<DiagnosticReportMedia>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/media/${number | "-"}`, value: DiagnosticReportMedia): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments about the diagnostic report
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Responsible Diagnostic Service
   */
  add(
    path: "/performer",
    value: Array<
      Reference<Practitioner | PractitionerRole | Organization | CareTeam>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: Reference<Practitioner | PractitionerRole | Organization | CareTeam>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Entire report as issued
   */
  add(path: "/presentedForm", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/presentedForm/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Observations
   */
  add(path: "/result", value: Array<Reference<Observation>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/result/${number | "-"}`, value: Reference<Observation>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Primary result interpreter
   */
  add(
    path: "/resultsInterpreter",
    value: Array<
      Reference<Practitioner | PractitionerRole | Organization | CareTeam>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/resultsInterpreter/${number | "-"}`,
    value: Reference<Practitioner | PractitionerRole | Organization | CareTeam>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specimens this report is based on
   */
  add(path: "/specimen", value: Array<Reference<Specimen>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specimen/${number | "-"}`, value: Reference<Specimen>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: DiagnosticReportStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reference to full details of an analysis associated with the diagnostic report
   */
  add(
    path: "/study",
    value: Array<Reference<GenomicStudy | ImagingStudy>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/study/${number | "-"}`,
    value: Reference<GenomicStudy | ImagingStudy>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | Group
      | Device
      | Location
      | Organization
      | Practitioner
      | Medication
      | Substance
      | BiologicallyDerivedProduct
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information supporting the diagnostic report
   */
  add(
    path: "/supportingInfo",
    value: Array<DiagnosticReportSupportingInfo>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: DiagnosticReportSupportingInfo
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface DocumentReferenceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "DocumentReference";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Attests to accuracy of the document
   */
  add(path: "/attester", value: Array<DocumentReferenceAttester>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/attester/${number | "-"}`,
    value: DocumentReferenceAttester
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who and/or what authored the document
   */
  add(
    path: "/author",
    value: Array<
      Reference<
        | Practitioner
        | PractitionerRole
        | Organization
        | Device
        | Patient
        | RelatedPerson
        | CareTeam
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/author/${number | "-"}`,
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | Device
      | Patient
      | RelatedPerson
      | CareTeam
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Procedure that caused this media to be created
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        | Appointment
        | AppointmentResponse
        | CarePlan
        | Claim
        | CommunicationRequest
        | Contract
        | CoverageEligibilityRequest
        | DeviceRequest
        | EnrollmentRequest
        | ImmunizationRecommendation
        | MedicationRequest
        | NutritionOrder
        | RequestOrchestration
        | ServiceRequest
        | SupplyRequest
        | VisionPrescription
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      | Appointment
      | AppointmentResponse
      | CarePlan
      | Claim
      | CommunicationRequest
      | Contract
      | CoverageEligibilityRequest
      | DeviceRequest
      | EnrollmentRequest
      | ImmunizationRecommendation
      | MedicationRequest
      | NutritionOrder
      | RequestOrchestration
      | ServiceRequest
      | SupplyRequest
      | VisionPrescription
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Body part included
   */
  add(path: "/bodySite", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/bodySite/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Categorization of document
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Document referenced
   */
  add(path: "/content", value: Array<DocumentReferenceContent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/content/${number | "-"}`, value: DocumentReferenceContent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Context of the document content
   */
  add(
    path: "/context",
    value: Array<Reference<Appointment | Encounter | EpisodeOfCare>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/context/${number | "-"}`,
    value: Reference<Appointment | Encounter | EpisodeOfCare>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/custodian", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/docStatus", value: CompositionStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Main clinical acts documented
   */
  add(path: "/event", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/event/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/facilityType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifiers for the document
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Imaging modality used
   */
  add(path: "/modality", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/modality/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/practiceSetting", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Relationships to other documents
   */
  add(path: "/relatesTo", value: Array<DocumentReferenceRelatesTo>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relatesTo/${number | "-"}`,
    value: DocumentReferenceRelatesTo
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Document security-tags
   */
  add(path: "/securityLabel", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/securityLabel/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: DocumentReferenceStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EncounterFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Encounter";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The set of accounts that may be used for billing for this Encounter
   */
  add(path: "/account", value: Array<Reference<Account>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/account/${number | "-"}`, value: Reference<Account>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/actualPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/admission", value: EncounterAdmission): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The appointment that scheduled this encounter
   */
  add(path: "/appointment", value: Array<Reference<Appointment>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/appointment/${number | "-"}`,
    value: Reference<Appointment>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The request that initiated this encounter
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<CarePlan | DeviceRequest | MedicationRequest | ServiceRequest>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      CarePlan | DeviceRequest | MedicationRequest | ServiceRequest
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The group(s) that are allocated to participate in this encounter
   */
  add(path: "/careTeam", value: Array<Reference<CareTeam>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/careTeam/${number | "-"}`, value: Reference<CareTeam>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classification of patient encounter context - e.g. Inpatient, outpatient
   */
  add(path: "/class", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/class/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The list of diagnosis relevant to this encounter
   */
  add(path: "/diagnosis", value: Array<EncounterDiagnosis>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/diagnosis/${number | "-"}`, value: EncounterDiagnosis): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Diet preferences reported by the patient
   */
  add(path: "/dietPreference", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/dietPreference/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Episode(s) of care that this encounter should be recorded against
   */
  add(path: "/episodeOfCare", value: Array<Reference<EpisodeOfCare>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/episodeOfCare/${number | "-"}`,
    value: Reference<EpisodeOfCare>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifier(s) by which this encounter is known
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/length", value: Duration): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * List of locations where the patient has been
   */
  add(path: "/location", value: Array<EncounterLocation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/location/${number | "-"}`, value: EncounterLocation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * List of participants involved in the encounter
   */
  add(path: "/participant", value: Array<EncounterParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/participant/${number | "-"}`, value: EncounterParticipant): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/partOf", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/plannedEndDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/plannedStartDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The list of medical reasons that are expected to be addressed during the episode of care
   */
  add(path: "/reason", value: Array<EncounterReason>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: EncounterReason): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/serviceProvider", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific type of service
   */
  add(path: "/serviceType", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/serviceType/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Wheelchair, translator, stretcher, etc
   */
  add(path: "/specialArrangement", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/specialArrangement/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Special courtesies (VIP, board member)
   */
  add(path: "/specialCourtesy", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specialCourtesy/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: EncounterStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific type of encounter (e.g. e-mail consultation, surgical day-care, ...)
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Connection details of a virtual service (e.g. conference call)
   */
  add(path: "/virtualService", value: Array<VirtualServiceDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/virtualService/${number | "-"}`,
    value: VirtualServiceDetail
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EncounterHistoryFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "EncounterHistory";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/actualPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/class", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifier(s) by which this encounter is known
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/length", value: Duration): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Location of the patient at this point in the encounter
   */
  add(path: "/location", value: Array<EncounterHistoryLocation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/location/${number | "-"}`, value: EncounterHistoryLocation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/plannedEndDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/plannedStartDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific type of service
   */
  add(path: "/serviceType", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/serviceType/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: EncounterStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific type of encounter
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EndpointFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Endpoint";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/address", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Protocol/Profile/Standard to be used with this endpoint connection
   */
  add(path: "/connectionType", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/connectionType/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for source (e.g. troubleshooting)
   */
  add(path: "/contact", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactPoint): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The type of environment(s) exposed at this endpoint
   */
  add(path: "/environmentType", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/environmentType/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Usage depends on the channel type
   */
  add(path: "/header", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/header/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifies this endpoint across multiple systems
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/managingOrganization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Set of payloads that are provided by this endpoint
   */
  add(path: "/payload", value: Array<EndpointPayload>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/payload/${number | "-"}`, value: EndpointPayload): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: EndpointStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EnrollmentRequestFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "EnrollmentRequest";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/candidate", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/coverage", value: Reference<Coverage>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/insurer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/provider",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EnrollmentResponseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "EnrollmentResponse";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/disposition", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/organization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outcome", value: EnrollmentOutcome): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/request", value: Reference<EnrollmentRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requestProvider",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EpisodeOfCareFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "EpisodeOfCare";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The set of accounts that may be used for billing for this EpisodeOfCare
   */
  add(path: "/account", value: Array<Reference<Account>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/account/${number | "-"}`, value: Reference<Account>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/careManager",
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Other practitioners facilitating this episode of care
   */
  add(path: "/careTeam", value: Array<Reference<CareTeam>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/careTeam/${number | "-"}`, value: Reference<CareTeam>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The list of medical conditions that were addressed during the episode of care
   */
  add(path: "/diagnosis", value: Array<EpisodeOfCareDiagnosis>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/diagnosis/${number | "-"}`, value: EpisodeOfCareDiagnosis): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier(s) relevant for this EpisodeOfCare
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/managingOrganization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The list of medical reasons that are expected to be addressed during the episode of care
   */
  add(path: "/reason", value: Array<EpisodeOfCareReason>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: EpisodeOfCareReason): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Originating Referral Request(s)
   */
  add(path: "/referralRequest", value: Array<Reference<ServiceRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/referralRequest/${number | "-"}`,
    value: Reference<ServiceRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: EpisodeOfCareStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Past list of status codes (the current status may be included to cover the start date of the status)
   */
  add(path: "/statusHistory", value: Array<EpisodeOfCareStatusHistory>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/statusHistory/${number | "-"}`,
    value: EpisodeOfCareStatusHistory
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type/class  - e.g. specialist referral, disease management
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EventDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "EventDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the content
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the content
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the content
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the event definition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for event definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the content
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectReference", value: Reference<Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subtitle", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * "when" the event occurs (multiple = 'or')
   */
  add(path: "/trigger", value: Array<TriggerDefinition>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/trigger/${number | "-"}`, value: TriggerDefinition): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usage", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EvidenceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Evidence";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/assertion", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the content
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Certainty or quality of the evidence
   */
  add(path: "/certainty", value: Array<EvidenceCertainty>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/certainty/${number | "-"}`, value: EvidenceCertainty): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/citeAsReference", value: Reference<Citation>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/citeAsMarkdown", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the content
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the content
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the summary
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Footnotes and/or explanatory notes
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Link or citation to artifact associated with the summary
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the content
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Values and parameters for a single statistic
   */
  add(path: "/statistic", value: Array<EvidenceStatistic>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/statistic/${number | "-"}`, value: EvidenceStatistic): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The design of the study that produced this evidence
   */
  add(path: "/studyDesign", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/studyDesign/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/synthesisType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Evidence variable such as population, exposure, or outcome
   */
  add(
    path: "/variableDefinition",
    value: Array<EvidenceVariableDefinition>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/variableDefinition/${number | "-"}`,
    value: EvidenceVariableDefinition
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EvidenceReportFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "EvidenceReport";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the content
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/citeAsReference", value: Reference<Citation>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/citeAsMarkdown", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the content
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the content
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique identifier for the evidence report
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Used for footnotes and annotations
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Link, description or reference to artifact associated with the report
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifiers for articles that may relate to more than one evidence report
   */
  add(path: "/relatedIdentifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedIdentifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Relationships to other compositions/documents
   */
  add(path: "/relatesTo", value: Array<EvidenceReportRelatesTo>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatesTo/${number | "-"}`, value: EvidenceReportRelatesTo): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the content
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Composition is broken into sections
   */
  add(path: "/section", value: Array<EvidenceReportSection>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/section/${number | "-"}`, value: EvidenceReportSection): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: EvidenceReportSubject): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface EvidenceVariableFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "EvidenceVariable";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/actual", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the content
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A grouping for ordinal or polychotomous variables
   */
  add(path: "/category", value: Array<EvidenceVariableCategory>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: EvidenceVariableCategory): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A defining factor of the EvidenceVariable
   */
  add(
    path: "/characteristic",
    value: Array<EvidenceVariableCharacteristic>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/characteristic/${number | "-"}`,
    value: EvidenceVariableCharacteristic
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the content
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the content
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/handling", value: EvidenceVariableHandling): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the evidence variable
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Used for footnotes or explanatory notes
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the content
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/shortTitle", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ExampleScenarioFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ExampleScenario";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Individual involved in exchange
   */
  add(path: "/actor", value: Array<ExampleScenarioActor>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/actor/${number | "-"}`, value: ExampleScenarioActor): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the example scenario
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Data used in the scenario
   */
  add(path: "/instance", value: Array<ExampleScenarioInstance>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instance/${number | "-"}`, value: ExampleScenarioInstance): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for example scenario (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Major process within scenario
   */
  add(path: "/process", value: Array<ExampleScenarioProcess>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/process/${number | "-"}`, value: ExampleScenarioProcess): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ExplanationOfBenefitFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ExplanationOfBenefit";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/accident", value: ExplanationOfBenefitAccident): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Insurer added line items
   */
  add(path: "/addItem", value: Array<ExplanationOfBenefitAddItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/addItem/${number | "-"}`,
    value: ExplanationOfBenefitAddItem
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Balance by Benefit Category
   */
  add(
    path: "/benefitBalance",
    value: Array<ExplanationOfBenefitBenefitBalance>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/benefitBalance/${number | "-"}`,
    value: ExplanationOfBenefitBenefitBalance
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/benefitPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/billablePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Care Team members
   */
  add(path: "/careTeam", value: Array<ExplanationOfBenefitCareTeam>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/careTeam/${number | "-"}`,
    value: ExplanationOfBenefitCareTeam
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/claim", value: Reference<Claim>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/claimResponse", value: Reference<ClaimResponse>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/decision", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Pertinent diagnosis information
   */
  add(path: "/diagnosis", value: Array<ExplanationOfBenefitDiagnosis>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/diagnosis/${number | "-"}`,
    value: ExplanationOfBenefitDiagnosis
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/diagnosisRelatedGroup", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/disposition", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Encounters associated with the listed treatments
   */
  add(path: "/encounter", value: Array<Reference<Encounter>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/encounter/${number | "-"}`, value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/enterer",
    value: Reference<Practitioner | PractitionerRole | Patient | RelatedPerson>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Event information
   */
  add(path: "/event", value: Array<ExplanationOfBenefitEvent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/event/${number | "-"}`, value: ExplanationOfBenefitEvent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/facility", value: Reference<Location | Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/form", value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/formCode", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/fundsReserve", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/fundsReserveRequested", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for the resource
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient insurance information
   */
  add(path: "/insurance", value: Array<ExplanationOfBenefitInsurance>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/insurance/${number | "-"}`,
    value: ExplanationOfBenefitInsurance
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/insurer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Product or service provided
   */
  add(path: "/item", value: Array<ExplanationOfBenefitItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/item/${number | "-"}`, value: ExplanationOfBenefitItem): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/originalPrescription", value: Reference<MedicationRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outcome", value: ClaimProcessingCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patientPaid", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/payee", value: ExplanationOfBenefitPayee): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/payment", value: ExplanationOfBenefitPayment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Preauthorization reference
   */
  add(path: "/preAuthRef", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/preAuthRef/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Preauthorization in-effect period
   */
  add(path: "/preAuthRefPeriod", value: Array<Period>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/preAuthRefPeriod/${number | "-"}`, value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/precedence", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/prescription",
    value: Reference<MedicationRequest | VisionPrescription>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Clinical procedures performed
   */
  add(path: "/procedure", value: Array<ExplanationOfBenefitProcedure>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/procedure/${number | "-"}`,
    value: ExplanationOfBenefitProcedure
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Note concerning adjudication
   */
  add(
    path: "/processNote",
    value: Array<ExplanationOfBenefitProcessNote>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/processNote/${number | "-"}`,
    value: ExplanationOfBenefitProcessNote
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/provider",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/referral", value: Reference<ServiceRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Prior or corollary claims
   */
  add(path: "/related", value: Array<ExplanationOfBenefitRelated>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/related/${number | "-"}`,
    value: ExplanationOfBenefitRelated
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ExplanationOfBenefitStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supporting information
   */
  add(
    path: "/supportingInfo",
    value: Array<ExplanationOfBenefitSupportingInfo>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: ExplanationOfBenefitSupportingInfo
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Adjudication totals
   */
  add(path: "/total", value: Array<ExplanationOfBenefitTotal>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/total/${number | "-"}`, value: ExplanationOfBenefitTotal): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Number for tracking
   */
  add(path: "/traceNumber", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/traceNumber/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/use", value: Use): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface FamilyMemberHistoryFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "FamilyMemberHistory";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/ageAge", value: Age): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/ageRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/ageString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bornPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bornDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bornString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Condition that the related person had
   */
  add(path: "/condition", value: Array<FamilyMemberHistoryCondition>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/condition/${number | "-"}`,
    value: FamilyMemberHistoryCondition
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dataAbsentReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedAge", value: Age): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/estimatedAge", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Id(s) for this record
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * General note about related person
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who or what participated in the activities related to the family member history and how they were involved
   */
  add(path: "/participant", value: Array<FamilyMemberHistoryParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/participant/${number | "-"}`,
    value: FamilyMemberHistoryParticipant
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Procedures that the related person had
   */
  add(path: "/procedure", value: Array<FamilyMemberHistoryProcedure>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/procedure/${number | "-"}`,
    value: FamilyMemberHistoryProcedure
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why was family member history performed?
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/relationship", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sex", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FamilyHistoryStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface FlagFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Flag";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/author",
    value: Reference<
      | Device
      | Organization
      | Patient
      | RelatedPerson
      | Practitioner
      | PractitionerRole
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Clinical, administrative, etc
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FlagStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | RelatedPerson
      | Location
      | Group
      | Organization
      | Practitioner
      | PractitionerRole
      | PlanDefinition
      | Medication
      | Procedure
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface FormularyItemFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "FormularyItem";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for this formulary item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FormularyItemStatusCodes): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface GenomicStudyFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "GenomicStudy";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Genomic Analysis Event
   */
  add(path: "/analysis", value: Array<GenomicStudyAnalysis>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/analysis/${number | "-"}`, value: GenomicStudyAnalysis): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Event resources that the genomic study is based on
   */
  add(path: "/basedOn", value: Array<Reference<ServiceRequest | Task>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<ServiceRequest | Task>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifiers for this genomic study
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Healthcare professionals who interpreted the genomic study
   */
  add(
    path: "/interpreter",
    value: Array<Reference<Practitioner | PractitionerRole>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/interpreter/${number | "-"}`,
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments related to the genomic study
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why the genomic study was performed
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/referrer",
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/startDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: GenomicStudyStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | Group
      | Substance
      | BiologicallyDerivedProduct
      | NutritionProduct
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The type of the study (e.g., Familial variant segregation, Functional variation detection, or Gene expression profiling)
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface GoalFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Goal";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/achievementStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Issues addressed by this goal
   */
  add(
    path: "/addresses",
    value: Array<
      Reference<
        | Condition
        | Observation
        | MedicationStatement
        | MedicationRequest
        | NutritionOrder
        | ServiceRequest
        | RiskAssessment
        | Procedure
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/addresses/${number | "-"}`,
    value: Reference<
      | Condition
      | Observation
      | MedicationStatement
      | MedicationRequest
      | NutritionOrder
      | ServiceRequest
      | RiskAssessment
      | Procedure
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Treatment, dietary, behavioral, etc
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/continuous", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Ids for this goal
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lifecycleStatus", value: GoalLifecycleStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments about the goal
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What result was achieved regarding the goal?
   */
  add(path: "/outcome", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/outcome/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/source",
    value: Reference<
      Patient | Practitioner | PractitionerRole | RelatedPerson | CareTeam
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/startDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/startCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group | Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Target outcome for the goal
   */
  add(path: "/target", value: Array<GoalTarget>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/target/${number | "-"}`, value: GoalTarget): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface GraphDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "GraphDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the GraphDefinition (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for graph definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Links this graph makes rules about
   */
  add(path: "/link", value: Array<GraphDefinitionLink>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/link/${number | "-"}`, value: GraphDefinitionLink): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Potential target for the link
   */
  add(path: "/node", value: Array<GraphDefinitionNode>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/node/${number | "-"}`, value: GraphDefinitionNode): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/start", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface GroupFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Group";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Include / Exclude group members by Trait
   */
  add(path: "/characteristic", value: Array<GroupCharacteristic>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/characteristic/${number | "-"}`,
    value: GroupCharacteristic
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for this Group
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/managingEntity",
    value: Reference<
      Organization | RelatedPerson | Practitioner | PractitionerRole
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who or what is in group
   */
  add(path: "/member", value: Array<GroupMember>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/member/${number | "-"}`, value: GroupMember): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/membership", value: GroupMembershipBasis): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: GroupType): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface GuidanceResponseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "GuidanceResponse";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional required data
   */
  add(path: "/dataRequirement", value: Array<DataRequirement>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/dataRequirement/${number | "-"}`, value: DataRequirement): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/evaluationMessage", value: Reference<OperationOutcome>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/moduleUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/moduleCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/moduleCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional notes about the response
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outputParameters", value: Reference<Parameters>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/performer", value: Reference<Device>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why guidance is needed
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/requestIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Proposed actions, if any
   */
  add(
    path: "/result",
    value: Array<
      Reference<
        | Appointment
        | AppointmentResponse
        | CarePlan
        | Claim
        | CommunicationRequest
        | Contract
        | CoverageEligibilityRequest
        | DeviceRequest
        | EnrollmentRequest
        | ImmunizationRecommendation
        | MedicationRequest
        | NutritionOrder
        | RequestOrchestration
        | ServiceRequest
        | SupplyRequest
        | Task
        | VisionPrescription
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/result/${number | "-"}`,
    value: Reference<
      | Appointment
      | AppointmentResponse
      | CarePlan
      | Claim
      | CommunicationRequest
      | Contract
      | CoverageEligibilityRequest
      | DeviceRequest
      | EnrollmentRequest
      | ImmunizationRecommendation
      | MedicationRequest
      | NutritionOrder
      | RequestOrchestration
      | ServiceRequest
      | SupplyRequest
      | Task
      | VisionPrescription
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: GuidanceResponseStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface HealthcareServiceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "HealthcareService";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/appointmentRequired", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Times the healthcare service is available (including exceptions)
   */
  add(path: "/availability", value: Array<Availability>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/availability/${number | "-"}`, value: Availability): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Broad category of service being performed or delivered
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Collection of characteristics (attributes)
   */
  add(path: "/characteristic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/characteristic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/comment", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The language that this service is offered in
   */
  add(path: "/communication", value: Array<AllLanguages>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/communication/${number | "-"}`, value: AllLanguages): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Official contact details for the HealthcareService
   */
  add(path: "/contact", value: Array<ExtendedContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ExtendedContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Location(s) service is intended for/available to
   */
  add(path: "/coverageArea", value: Array<Reference<Location>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/coverageArea/${number | "-"}`, value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific eligibility requirements required to use the service
   */
  add(path: "/eligibility", value: Array<HealthcareServiceEligibility>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/eligibility/${number | "-"}`,
    value: HealthcareServiceEligibility
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Technical endpoints providing access to electronic services operated for the healthcare service
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/extraDetails", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External identifiers for this item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Location(s) where service may be provided
   */
  add(path: "/location", value: Array<Reference<Location>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/location/${number | "-"}`, value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The service within which this service is offered
   */
  add(path: "/offeredIn", value: Array<Reference<HealthcareService>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/offeredIn/${number | "-"}`,
    value: Reference<HealthcareService>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/photo", value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Programs that this service is applicable to
   */
  add(path: "/program", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/program/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/providedBy", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Ways that the service accepts referrals
   */
  add(path: "/referralMethod", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/referralMethod/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Conditions under which service is available/offered
   */
  add(path: "/serviceProvisionCode", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/serviceProvisionCode/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specialties handled by the HealthcareService
   */
  add(path: "/specialty", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specialty/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of service that may be delivered or performed
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ImagingSelectionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ImagingSelection";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Associated request
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        CarePlan | ServiceRequest | Appointment | AppointmentResponse | Task
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      CarePlan | ServiceRequest | Appointment | AppointmentResponse | Task
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bodySite", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classifies the imaging selection
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The imaging study from which the imaging selection is derived
   */
  add(
    path: "/derivedFrom",
    value: Array<Reference<ImagingStudy | DocumentReference>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/derivedFrom/${number | "-"}`,
    value: Reference<ImagingStudy | DocumentReference>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The network service providing retrieval for the images referenced in the imaging selection
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Related resource that is the focus for the imaging selection
   */
  add(path: "/focus", value: Array<Reference<ImagingSelection>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/focus/${number | "-"}`, value: Reference<ImagingSelection>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/frameOfReferenceUid", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for Imaging Selection
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The selected instances
   */
  add(path: "/instance", value: Array<ImagingSelectionInstance>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instance/${number | "-"}`, value: ImagingSelectionInstance): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/issued", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Selector of the instances (human or machine)
   */
  add(path: "/performer", value: Array<ImagingSelectionPerformer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: ImagingSelectionPerformer
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/seriesNumber", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/seriesUid", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ImagingSelectionStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/studyUid", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | Group
      | Device
      | Location
      | Organization
      | Procedure
      | Practitioner
      | Medication
      | Substance
      | Specimen
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ImagingStudyFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ImagingStudy";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request fulfilled
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        CarePlan | ServiceRequest | Appointment | AppointmentResponse | Task
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      CarePlan | ServiceRequest | Appointment | AppointmentResponse | Task
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Study access endpoint
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifiers for the whole study
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * All of the distinct values for series' modalities
   */
  add(path: "/modality", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/modality/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * User-defined comments
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/numberOfInstances", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/numberOfSeries", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(path: "/partOf", value: Array<Reference<Procedure>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: Reference<Procedure>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The performed procedure or code
   */
  add(path: "/procedure", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/procedure/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why the study was requested / performed
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/referrer",
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Each study has one or more series of instances
   */
  add(path: "/series", value: Array<ImagingStudySeries>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/series/${number | "-"}`, value: ImagingStudySeries): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/started", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ImagingStudyStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Device | Group>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ImmunizationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Immunization";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/administeredProduct", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Authority that the immunization event is based on
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        | CarePlan
        | MedicationRequest
        | ServiceRequest
        | ImmunizationRecommendation
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      CarePlan | MedicationRequest | ServiceRequest | ImmunizationRecommendation
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doseQuantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expirationDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/fundingSource", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/informationSource", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/isSubpotent", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lotNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/manufacturer", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional immunization notes
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who performed event
   */
  add(path: "/performer", value: Array<ImmunizationPerformer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/performer/${number | "-"}`, value: ImmunizationPerformer): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/primarySource", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient eligibility for a specific vaccination program
   */
  add(
    path: "/programEligibility",
    value: Array<ImmunizationProgramEligibility>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/programEligibility/${number | "-"}`,
    value: ImmunizationProgramEligibility
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Protocol followed by the provider
   */
  add(
    path: "/protocolApplied",
    value: Array<ImmunizationProtocolApplied>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/protocolApplied/${number | "-"}`,
    value: ImmunizationProtocolApplied
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Details of a reaction that follows immunization
   */
  add(path: "/reaction", value: Array<ImmunizationReaction>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reaction/${number | "-"}`, value: ImmunizationReaction): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why immunization occurred
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/route", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/site", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ImmunizationStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason for being subpotent
   */
  add(path: "/subpotentReason", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subpotentReason/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information in support of the immunization
   */
  add(path: "/supportingInformation", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInformation/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/vaccineCode", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ImmunizationEvaluationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ImmunizationEvaluation";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authority", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doseNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doseStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason why the doese is considered valid, invalid or some other status
   */
  add(path: "/doseStatusReason", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/doseStatusReason/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/immunizationEvent", value: Reference<Immunization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/series", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/seriesDoses", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ImmunizationEvaluationStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/targetDisease", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ImmunizationRecommendationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ImmunizationRecommendation";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authority", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Vaccine administration recommendations
   */
  add(
    path: "/recommendation",
    value: Array<ImmunizationRecommendationRecommendation>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/recommendation/${number | "-"}`,
    value: ImmunizationRecommendationRecommendation
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ImplementationGuideFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ImplementationGuide";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/definition", value: ImplementationGuideDefinition): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Another Implementation guide this depends on
   */
  add(path: "/dependsOn", value: Array<ImplementationGuideDependsOn>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/dependsOn/${number | "-"}`,
    value: ImplementationGuideDependsOn
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * FHIR Version(s) this Implementation Guide targets
   */
  add(path: "/fhirVersion", value: Array<FHIRVersion>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/fhirVersion/${number | "-"}`, value: FHIRVersion): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Profiles that apply globally
   */
  add(path: "/global", value: Array<ImplementationGuideGlobal>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/global/${number | "-"}`, value: ImplementationGuideGlobal): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the implementation guide (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for implementation guide (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/license", value: SPDXLicense): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/manifest", value: ImplementationGuideManifest): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/packageId", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface IngredientFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Ingredient";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/allergenicIndicator", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/comment", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The product which this ingredient is a constituent part of
   */
  add(
    path: "/for",
    value: Array<
      Reference<
        | MedicinalProductDefinition
        | AdministrableProductDefinition
        | ManufacturedItemDefinition
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/for/${number | "-"}`,
    value: Reference<
      | MedicinalProductDefinition
      | AdministrableProductDefinition
      | ManufacturedItemDefinition
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Precise action within the drug product, e.g. antioxidant, alkalizing agent
   */
  add(path: "/function", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/function/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/group", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/identifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An organization that manufactures this ingredient
   */
  add(path: "/manufacturer", value: Array<IngredientManufacturer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/manufacturer/${number | "-"}`,
    value: IngredientManufacturer
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/role", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/substance", value: IngredientSubstance): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface InsurancePlanFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "InsurancePlan";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/administeredBy", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Alternate names
   */
  add(path: "/alias", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/alias/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Official contact details relevant to the health insurance plan/product
   */
  add(path: "/contact", value: Array<ExtendedContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ExtendedContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Coverage details
   */
  add(path: "/coverage", value: Array<InsurancePlanCoverage>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/coverage/${number | "-"}`, value: InsurancePlanCoverage): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Where product applies
   */
  add(path: "/coverageArea", value: Array<Reference<Location>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/coverageArea/${number | "-"}`, value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Technical endpoint
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for Product
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What networks are Included
   */
  add(path: "/network", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/network/${number | "-"}`, value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/ownedBy", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Plan details
   */
  add(path: "/plan", value: Array<InsurancePlanPlan>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/plan/${number | "-"}`, value: InsurancePlanPlan): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Kind of product
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface InventoryItemFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "InventoryItem";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Association with other items or products
   */
  add(path: "/association", value: Array<InventoryItemAssociation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/association/${number | "-"}`,
    value: InventoryItemAssociation
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/baseUnit", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Category or class of the item
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Characteristic of the item
   */
  add(path: "/characteristic", value: Array<InventoryItemCharacteristic>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/characteristic/${number | "-"}`,
    value: InventoryItemCharacteristic
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Code designating the specific type of item
   */
  add(path: "/code", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/code/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: InventoryItemDescription): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for the inventory item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instance", value: InventoryItemInstance): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The usage status like recalled, in use, discarded
   */
  add(path: "/inventoryStatus", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/inventoryStatus/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The item name(s) - the brand name, or common name, functional name, generic name or others
   */
  add(path: "/name", value: Array<InventoryItemName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/name/${number | "-"}`, value: InventoryItemName): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/netContent", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/productReference",
    value: Reference<
      Medication | Device | NutritionProduct | BiologicallyDerivedProduct
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Organization(s) responsible for the product
   */
  add(
    path: "/responsibleOrganization",
    value: Array<InventoryItemResponsibleOrganization>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/responsibleOrganization/${number | "-"}`,
    value: InventoryItemResponsibleOrganization
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: InventoryItemStatusCodes): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface InventoryReportFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "InventoryReport";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/countType", value: InventoryCountType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for the report
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An inventory listing section (grouped by any of the attributes)
   */
  add(
    path: "/inventoryListing",
    value: Array<InventoryReportInventoryListing>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/inventoryListing/${number | "-"}`,
    value: InventoryReportInventoryListing
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A note associated with the InventoryReport
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/operationType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/operationTypeReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reportedDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/reporter",
    value: Reference<Practitioner | Patient | RelatedPerson | Device>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reportingPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: InventoryReportStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface InvoiceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Invoice";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/account", value: Reference<Account>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/cancelledReason", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/creation", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/issuer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Line items of this Invoice
   */
  add(path: "/lineItem", value: Array<InvoiceLineItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/lineItem/${number | "-"}`, value: InvoiceLineItem): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the invoice
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Participant in creation of this Invoice
   */
  add(path: "/participant", value: Array<InvoiceParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/participant/${number | "-"}`, value: InvoiceParticipant): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/paymentTerms", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/periodDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/periodPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/recipient",
    value: Reference<Organization | Patient | RelatedPerson>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: InvoiceStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/totalGross", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/totalNet", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Components of Invoice total
   */
  add(path: "/totalPriceComponent", value: Array<MonetaryComponent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/totalPriceComponent/${number | "-"}`,
    value: MonetaryComponent
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface LibraryFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Library";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the content
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contents of the library, either embedded or referenced
   */
  add(path: "/content", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/content/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What data is referenced by this library
   */
  add(path: "/dataRequirement", value: Array<DataRequirement>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/dataRequirement/${number | "-"}`, value: DataRequirement): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the content
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the content
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the library
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for library (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Parameters defined by the library
   */
  add(path: "/parameter", value: Array<ParameterDefinition>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parameter/${number | "-"}`, value: ParameterDefinition): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the content
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectReference", value: Reference<Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subtitle", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usage", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface LinkageFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Linkage";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/author",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Item to be linked
   */
  add(path: "/item", value: Array<LinkageItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/item/${number | "-"}`, value: LinkageItem): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ListFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "List";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/emptyReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Entries in the list
   */
  add(path: "/entry", value: Array<ListEntry>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/entry/${number | "-"}`, value: ListEntry): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/mode", value: ListMode): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments about the list
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/orderedBy", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/source",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Patient
      | Device
      | Organization
      | RelatedPerson
      | CareTeam
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ListStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * If all resources have the same subject(s)
   */
  add(path: "/subject", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subject/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface LocationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Location";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/address", value: Address): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A list of alternate names that the location is known as, or was known as, in the past
   */
  add(path: "/alias", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/alias/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Collection of characteristics (attributes)
   */
  add(path: "/characteristic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/characteristic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Official contact details for the location
   */
  add(path: "/contact", value: Array<ExtendedContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ExtendedContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Technical endpoints providing access to services operated for the location
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/form", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What days/times during a week is this location usually open (including exceptions)
   */
  add(path: "/hoursOfOperation", value: Array<Availability>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/hoursOfOperation/${number | "-"}`, value: Availability): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique code or number identifying the location to its users
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/managingOrganization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/mode", value: LocationMode): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/operationalStatus", value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/partOf", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/position", value: LocationPosition): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: LocationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of function performed
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Connection details of a virtual service (e.g. conference call)
   */
  add(path: "/virtualService", value: Array<VirtualServiceDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/virtualService/${number | "-"}`,
    value: VirtualServiceDetail
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ManufacturedItemDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ManufacturedItemDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Physical parts of the manufactured item, that it is intrisically made from. This is distinct from the ingredients that are part of its chemical makeup
   */
  add(
    path: "/component",
    value: Array<ManufacturedItemDefinitionComponent>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/component/${number | "-"}`,
    value: ManufacturedItemDefinitionComponent
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The ingredients of this manufactured item. Only needed if these are not specified by incoming references from the Ingredient resource
   */
  add(path: "/ingredient", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/ingredient/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/manufacturedDoseForm", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Manufacturer of the item, one of several possible
   */
  add(path: "/manufacturer", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/manufacturer/${number | "-"}`,
    value: Reference<Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Allows specifying that an item is on the market for sale, or that it is not available, and the dates and locations associated
   */
  add(path: "/marketingStatus", value: Array<MarketingStatus>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/marketingStatus/${number | "-"}`, value: MarketingStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * General characteristics of this item
   */
  add(
    path: "/property",
    value: Array<ManufacturedItemDefinitionProperty>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/property/${number | "-"}`,
    value: ManufacturedItemDefinitionProperty
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/unitOfPresentation", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MeasureFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Measure";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the content
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/basis", value: AllFHIRTypes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/clinicalRecommendationStatement", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/compositeScoring", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/disclaimer", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the content
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the content
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Population criteria group
   */
  add(path: "/group", value: Array<MeasureGroup>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/group/${number | "-"}`, value: MeasureGroup): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/guidance", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the measure
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/improvementNotation", value: MeasureImprovementNotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for measure (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Logic used by the measure
   */
  add(path: "/library", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/library/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/rateAggregation", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/rationale", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the content
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/riskAdjustment", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/scoring", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/scoringUnit", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectReference", value: Reference<Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subtitle", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What other data should be reported with the measure
   */
  add(path: "/supplementalData", value: Array<MeasureSupplementalData>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supplementalData/${number | "-"}`,
    value: MeasureSupplementalData
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Defined terms used in the measure documentation
   */
  add(path: "/term", value: Array<MeasureTerm>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/term/${number | "-"}`, value: MeasureTerm): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The category of the measure, such as Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * process | outcome | structure | patient-reported-outcome | composite
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usage", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MeasureReportFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MeasureReport";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dataUpdateType", value: SubmitDataUpdateType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What data was used to calculate the measure score
   */
  add(path: "/evaluatedResource", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/evaluatedResource/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Measure results for each group
   */
  add(path: "/group", value: Array<MeasureReportGroup>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/group/${number | "-"}`, value: MeasureReportGroup): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the MeasureReport
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/improvementNotation", value: MeasureImprovementNotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/inputParameters", value: Reference<Parameters>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/measure", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/reporter",
    value: Reference<Practitioner | PractitionerRole | Organization | Group>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reportingVendor", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/scoring", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: MeasureReportStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | CareTeam
      | Device
      | Group
      | HealthcareService
      | Location
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information collected for the report
   */
  add(path: "/supplementalData", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supplementalData/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: MeasureReportType): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MedicationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Medication";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/batch", value: MedicationBatch): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/definition", value: Reference<MedicationKnowledge>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doseForm", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for this medication
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Active or inactive ingredient
   */
  add(path: "/ingredient", value: Array<MedicationIngredient>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/ingredient/${number | "-"}`, value: MedicationIngredient): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/marketingAuthorizationHolder",
    value: Reference<Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: MedicationStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/totalVolume", value: Quantity): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MedicationAdministrationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MedicationAdministration";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Plan this is fulfilled by this administration
   */
  add(path: "/basedOn", value: Array<Reference<CarePlan>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<CarePlan>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of medication administration
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Device used to administer
   */
  add(path: "/device", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/device/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dosage", value: MedicationAdministrationDosage): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A list of events of interest in the lifecycle
   */
  add(path: "/eventHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/eventHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/isSubPotent", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/medication", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information about the administration
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurenceTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(
    path: "/partOf",
    value: Array<
      Reference<MedicationAdministration | Procedure | MedicationDispense>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<MedicationAdministration | Procedure | MedicationDispense>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who or what performed the medication administration and what type of performance they did
   */
  add(
    path: "/performer",
    value: Array<MedicationAdministrationPerformer>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: MedicationAdministrationPerformer
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Concept, condition or observation that supports why the medication was administered
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recorded", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/request", value: Reference<MedicationRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: MedicationAdministrationStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason administration not performed
   */
  add(path: "/statusReason", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/statusReason/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason full dose was not administered
   */
  add(path: "/subPotentReason", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subPotentReason/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information to support administration
   */
  add(path: "/supportingInformation", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInformation/${number | "-"}`,
    value: Reference<Resource>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MedicationDispenseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MedicationDispense";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Medication order that authorizes the dispense
   */
  add(
    path: "/authorizingPrescription",
    value: Array<Reference<MedicationRequest>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/authorizingPrescription/${number | "-"}`,
    value: Reference<MedicationRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Plan that is fulfilled by this dispense
   */
  add(path: "/basedOn", value: Array<Reference<CarePlan>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<CarePlan>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of medication dispense
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/daysSupply", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/destination", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * How the medication is to be used by the patient or administered by the caregiver
   */
  add(path: "/dosageInstruction", value: Array<Dosage>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/dosageInstruction/${number | "-"}`, value: Dosage): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A list of relevant lifecycle events
   */
  add(path: "/eventHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/eventHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/medication", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information about the dispense
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/notPerformedReason", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Event that dispense is part of
   */
  add(
    path: "/partOf",
    value: Array<Reference<Procedure | MedicationAdministration>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<Procedure | MedicationAdministration>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who performed event
   */
  add(path: "/performer", value: Array<MedicationDispensePerformer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: MedicationDispensePerformer
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who collected the medication or where the medication was delivered
   */
  add(
    path: "/receiver",
    value: Array<
      Reference<
        Patient | Practitioner | RelatedPerson | Location | PractitionerRole
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/receiver/${number | "-"}`,
    value: Reference<
      Patient | Practitioner | RelatedPerson | Location | PractitionerRole
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recorded", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/renderedDosageInstruction", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: MedicationDispenseStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusChanged", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/substitution", value: MedicationDispenseSubstitution): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information that supports the dispensing of the medication
   */
  add(path: "/supportingInformation", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInformation/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/whenHandedOver", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/whenPrepared", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MedicationKnowledgeFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MedicationKnowledge";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The set of medication resources that are associated with this medication
   */
  add(path: "/associatedMedication", value: Array<Reference<Medication>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/associatedMedication/${number | "-"}`,
    value: Reference<Medication>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/author", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Potential clinical issue with or between medication(s)
   */
  add(
    path: "/clinicalUseIssue",
    value: Array<Reference<ClinicalUseDefinition>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/clinicalUseIssue/${number | "-"}`,
    value: Reference<ClinicalUseDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The pricing of the medication
   */
  add(path: "/cost", value: Array<MedicationKnowledgeCost>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/cost/${number | "-"}`, value: MedicationKnowledgeCost): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/definitional", value: MedicationKnowledgeDefinitional): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for this medication
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Guidelines or protocols for administration of the medication for an indication
   */
  add(
    path: "/indicationGuideline",
    value: Array<MedicationKnowledgeIndicationGuideline>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/indicationGuideline/${number | "-"}`,
    value: MedicationKnowledgeIndicationGuideline
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Codes that identify the different jurisdictions for which the information of this resource was created
   */
  add(path: "/intendedJurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/intendedJurisdiction/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Categorization of the medication within a formulary or classification system
   */
  add(
    path: "/medicineClassification",
    value: Array<MedicationKnowledgeMedicineClassification>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/medicineClassification/${number | "-"}`,
    value: MedicationKnowledgeMedicineClassification
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Program under which a medication is reviewed
   */
  add(
    path: "/monitoringProgram",
    value: Array<MedicationKnowledgeMonitoringProgram>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/monitoringProgram/${number | "-"}`,
    value: MedicationKnowledgeMonitoringProgram
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Associated documentation about the medication
   */
  add(path: "/monograph", value: Array<MedicationKnowledgeMonograph>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/monograph/${number | "-"}`,
    value: MedicationKnowledgeMonograph
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A name associated with the medication being described
   */
  add(path: "/name", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/name/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Details about packaged medications
   */
  add(path: "/packaging", value: Array<MedicationKnowledgePackaging>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/packaging/${number | "-"}`,
    value: MedicationKnowledgePackaging
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/preparationInstruction", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Category of the medication or product
   */
  add(path: "/productType", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/productType/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Regulatory information about a medication
   */
  add(path: "/regulatory", value: Array<MedicationKnowledgeRegulatory>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/regulatory/${number | "-"}`,
    value: MedicationKnowledgeRegulatory
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Associated or related medication information
   */
  add(
    path: "/relatedMedicationKnowledge",
    value: Array<MedicationKnowledgeRelatedMedicationKnowledge>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relatedMedicationKnowledge/${number | "-"}`,
    value: MedicationKnowledgeRelatedMedicationKnowledge
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: MedicationKnowledgeStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * How the medication should be stored
   */
  add(
    path: "/storageGuideline",
    value: Array<MedicationKnowledgeStorageGuideline>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/storageGuideline/${number | "-"}`,
    value: MedicationKnowledgeStorageGuideline
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MedicationRequestFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MedicationRequest";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authoredOn", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A plan or request that is fulfilled in whole or in part by this medication request
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        | CarePlan
        | MedicationRequest
        | ServiceRequest
        | ImmunizationRecommendation
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      CarePlan | MedicationRequest | ServiceRequest | ImmunizationRecommendation
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Grouping or category of medication request
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/courseOfTherapyType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended type of device for the administration
   */
  add(path: "/device", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/device/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dispenseRequest", value: MedicationRequestDispenseRequest): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doNotPerform", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific instructions for how the medication should be taken
   */
  add(path: "/dosageInstruction", value: Array<Dosage>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/dosageInstruction/${number | "-"}`, value: Dosage): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectiveDosePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A list of events of interest in the lifecycle
   */
  add(path: "/eventHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/eventHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/groupIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External ids for this request
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The person or organization who provided the information about this request, if the source is someone other than the requestor
   */
  add(
    path: "/informationSource",
    value: Array<
      Reference<
        Patient | Practitioner | PractitionerRole | RelatedPerson | Organization
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/informationSource/${number | "-"}`,
    value: Reference<
      Patient | Practitioner | PractitionerRole | RelatedPerson | Organization
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Associated insurance coverage
   */
  add(
    path: "/insurance",
    value: Array<Reference<Coverage | ClaimResponse>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/insurance/${number | "-"}`,
    value: Reference<Coverage | ClaimResponse>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: MedicationRequestIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/medication", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information about the prescription
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended performer of administration
   */
  add(
    path: "/performer",
    value: Array<
      Reference<
        | Practitioner
        | PractitionerRole
        | Organization
        | Patient
        | DeviceDefinition
        | RelatedPerson
        | CareTeam
        | HealthcareService
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | Patient
      | DeviceDefinition
      | RelatedPerson
      | CareTeam
      | HealthcareService
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/performerType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priorPrescription", value: Reference<MedicationRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason or indication for ordering or not ordering the medication
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/recorder",
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/renderedDosageInstruction", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reported", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requester",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | Patient
      | RelatedPerson
      | Device
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: MedicationrequestStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusChanged", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/substitution", value: MedicationRequestSubstitution): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information to support fulfilling of the medication
   */
  add(path: "/supportingInformation", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInformation/${number | "-"}`,
    value: Reference<Resource>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MedicationStatementFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MedicationStatement";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/adherence", value: MedicationStatementAdherence): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of medication statement
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dateAsserted", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Link to information used to derive the MedicationStatement
   */
  add(path: "/derivedFrom", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFrom/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Details of how medication is/was taken or should be taken
   */
  add(path: "/dosage", value: Array<Dosage>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/dosage/${number | "-"}`, value: Dosage): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectiveDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectiveTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Person or organization that provided the information about the taking of this medication
   */
  add(
    path: "/informationSource",
    value: Array<
      Reference<
        Patient | Practitioner | PractitionerRole | RelatedPerson | Organization
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/informationSource/${number | "-"}`,
    value: Reference<
      Patient | Practitioner | PractitionerRole | RelatedPerson | Organization
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/medication", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Further information about the usage
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(
    path: "/partOf",
    value: Array<Reference<Procedure | MedicationStatement>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<Procedure | MedicationStatement>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason for why the medication is being/was taken
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Link to information relevant to the usage of a medication
   */
  add(
    path: "/relatedClinicalInformation",
    value: Array<Reference<Observation | Condition>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relatedClinicalInformation/${number | "-"}`,
    value: Reference<Observation | Condition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/renderedDosageInstruction", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: MedicationStatementStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MedicinalProductDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MedicinalProductDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/additionalMonitoringIndicator", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation about the medicinal product
   */
  add(
    path: "/attachedDocument",
    value: Array<Reference<DocumentReference>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/attachedDocument/${number | "-"}`,
    value: Reference<DocumentReference>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Key product features such as "sugar free", "modified release"
   */
  add(
    path: "/characteristic",
    value: Array<MedicinalProductDefinitionCharacteristic>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/characteristic/${number | "-"}`,
    value: MedicinalProductDefinitionCharacteristic
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Allows the product to be classified by various systems
   */
  add(path: "/classification", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/classification/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Clinical trials or studies that this product is involved in
   */
  add(path: "/clinicalTrial", value: Array<Reference<ResearchStudy>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/clinicalTrial/${number | "-"}`,
    value: Reference<ResearchStudy>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A code that this product is known by, within some formal terminology
   */
  add(path: "/code", value: Array<Coding>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/code/${number | "-"}`, value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/combinedPharmaceuticalDoseForm", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Types of medicinal manufactured items and/or devices that this product consists of, such as tablets, capsule, or syringes
   */
  add(
    path: "/comprisedOf",
    value: Array<Reference<ManufacturedItemDefinition | DeviceDefinition>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/comprisedOf/${number | "-"}`,
    value: Reference<ManufacturedItemDefinition | DeviceDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A product specific contact, person (in a role), or an organization
   */
  add(path: "/contact", value: Array<MedicinalProductDefinitionContact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/contact/${number | "-"}`,
    value: MedicinalProductDefinitionContact
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reference to another product, e.g. for linking authorised to investigational product
   */
  add(
    path: "/crossReference",
    value: Array<MedicinalProductDefinitionCrossReference>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/crossReference/${number | "-"}`,
    value: MedicinalProductDefinitionCrossReference
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/domain", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for this product. Could be an MPID
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Any component of the drug product which is not the chemical entity defined as the drug substance, or an excipient in the drug product
   */
  add(path: "/impurity", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/impurity/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/indication", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The ingredients of this medicinal product - when not detailed in other resources
   */
  add(path: "/ingredient", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/ingredient/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/legalStatusOfSupply", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Marketing status of the medicinal product, in contrast to marketing authorization
   */
  add(path: "/marketingStatus", value: Array<MarketingStatus>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/marketingStatus/${number | "-"}`, value: MarketingStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A master file for the medicinal product (e.g. Pharmacovigilance System Master File)
   */
  add(path: "/masterFile", value: Array<Reference<DocumentReference>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/masterFile/${number | "-"}`,
    value: Reference<DocumentReference>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The product's name, including full name and possibly coded parts
   */
  add(path: "/name", value: Array<MedicinalProductDefinitionName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/name/${number | "-"}`,
    value: MedicinalProductDefinitionName
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A manufacturing or administrative process for the medicinal product
   */
  add(
    path: "/operation",
    value: Array<MedicinalProductDefinitionOperation>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/operation/${number | "-"}`,
    value: MedicinalProductDefinitionOperation
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Package type for the product
   */
  add(path: "/packagedMedicinalProduct", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/packagedMedicinalProduct/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/pediatricUseIndicator", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The path by which the product is taken into or makes contact with the body
   */
  add(path: "/route", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/route/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Whether the Medicinal Product is subject to special measures for regulatory reasons
   */
  add(path: "/specialMeasures", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specialMeasures/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MessageDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MessageDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Responses to this message
   */
  add(
    path: "/allowedResponse",
    value: Array<MessageDefinitionAllowedResponse>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/allowedResponse/${number | "-"}`,
    value: MessageDefinitionAllowedResponse
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/base", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/category", value: MessageSignificanceCategory): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/eventCoding", value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/eventUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Resource(s) that are the subject of the event
   */
  add(path: "/focus", value: Array<MessageDefinitionFocus>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/focus/${number | "-"}`, value: MessageDefinitionFocus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/graph", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for a given MessageDefinition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for message definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Protocol/workflow this is part of
   */
  add(path: "/parent", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parent/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Takes the place of
   */
  add(path: "/replaces", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/replaces/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/responseRequired", value: MessageheaderResponseRequest): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MessageHeaderFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MessageHeader";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/author",
    value: Reference<Practitioner | PractitionerRole | Device | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/definition", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Message destination application(s)
   */
  add(path: "/destination", value: Array<MessageHeaderDestination>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/destination/${number | "-"}`,
    value: MessageHeaderDestination
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/eventCoding", value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/eventCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The actual content of the message
   */
  add(path: "/focus", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/focus/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/response", value: MessageHeaderResponse): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/responsible",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/sender",
    value: Reference<Practitioner | PractitionerRole | Device | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/source", value: MessageHeaderSource): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MetadataResourceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MetadataResource";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the {{title}}
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the {{title}}
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the {{title}}
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the {{title}}
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface MolecularSequenceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "MolecularSequence";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/device", value: Reference<Device>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What the molecular sequence is about, when it is not about the subject of record
   */
  add(path: "/focus", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/focus/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Embedded file or a link (URL) which contains content to represent the sequence
   */
  add(path: "/formatted", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/formatted/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique ID for this particular sequence
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/literal", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/performer", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A sequence defined relative to another sequence
   */
  add(path: "/relative", value: Array<MolecularSequenceRelative>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relative/${number | "-"}`,
    value: MolecularSequenceRelative
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/specimen", value: Reference<Specimen>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | Group
      | Substance
      | BiologicallyDerivedProduct
      | NutritionProduct
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: SequenceType): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface NamingSystemFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "NamingSystem";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the CodeSystem
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the NamingSystem
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the NamingSystem
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the naming system (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for naming system (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/kind", value: NamingSystemType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/responsible", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the NamingSystem
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique identifiers used for system
   */
  add(path: "/uniqueId", value: Array<NamingSystemUniqueId>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/uniqueId/${number | "-"}`, value: NamingSystemUniqueId): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usage", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface NutritionIntakeFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "NutritionIntake";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Fulfils plan, proposal or order
   */
  add(
    path: "/basedOn",
    value: Array<Reference<NutritionOrder | CarePlan | ServiceRequest>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<NutritionOrder | CarePlan | ServiceRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What food or fluid product or item was consumed
   */
  add(path: "/consumedItem", value: Array<NutritionIntakeConsumedItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/consumedItem/${number | "-"}`,
    value: NutritionIntakeConsumedItem
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional supporting information
   */
  add(path: "/derivedFrom", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFrom/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Total nutrient for the whole meal, product, serving
   */
  add(
    path: "/ingredientLabel",
    value: Array<NutritionIntakeIngredientLabel>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/ingredientLabel/${number | "-"}`,
    value: NutritionIntakeIngredientLabel
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Further information about the consumption
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(
    path: "/partOf",
    value: Array<Reference<NutritionIntake | Procedure | Observation>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<NutritionIntake | Procedure | Observation>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who was performed in the intake
   */
  add(path: "/performer", value: Array<NutritionIntakePerformer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: NutritionIntakePerformer
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason for why the food or fluid is /was consumed
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recorded", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reportedBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/reportedReference",
    value: Reference<
      Patient | RelatedPerson | Practitioner | PractitionerRole | Organization
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: EventStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reason for current status
   */
  add(path: "/statusReason", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/statusReason/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface NutritionOrderFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "NutritionOrder";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * List of the patient's food and nutrition-related allergies and intolerances
   */
  add(
    path: "/allergyIntolerance",
    value: Array<Reference<AllergyIntolerance>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/allergyIntolerance/${number | "-"}`,
    value: Reference<AllergyIntolerance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What this order fulfills
   */
  add(
    path: "/basedOn",
    value: Array<Reference<CarePlan | NutritionOrder | ServiceRequest>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<CarePlan | NutritionOrder | ServiceRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/enteralFormula", value: NutritionOrderEnteralFormula): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Order-specific modifier about the type of food that should not be given
   */
  add(path: "/excludeFoodModifier", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/excludeFoodModifier/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Order-specific modifier about the type of food that should be given
   */
  add(path: "/foodPreferenceModifier", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/foodPreferenceModifier/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/groupIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifiers assigned to this order
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates protocol or definition
   */
  add(path: "/instantiates", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiates/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: RequestIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/oralDiet", value: NutritionOrderOralDiet): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/orderer",
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outsideFoodAllowed", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who is desired to perform the administration of what is being ordered
   */
  add(path: "/performer", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/performer/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: RequestStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supplement components
   */
  add(path: "/supplement", value: Array<NutritionOrderSupplement>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supplement/${number | "-"}`,
    value: NutritionOrderSupplement
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information to support fulfilling of the nutrition order
   */
  add(path: "/supportingInformation", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInformation/${number | "-"}`,
    value: Reference<Resource>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface NutritionProductFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "NutritionProduct";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Broad product groups or categories used to classify the product, such as Legume and Legume Products, Beverages, or Beef Products
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specifies descriptive properties of the nutrition product
   */
  add(
    path: "/characteristic",
    value: Array<NutritionProductCharacteristic>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/characteristic/${number | "-"}`,
    value: NutritionProductCharacteristic
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Ingredients contained in this product
   */
  add(path: "/ingredient", value: Array<NutritionProductIngredient>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/ingredient/${number | "-"}`,
    value: NutritionProductIngredient
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * One or several physical instances or occurrences of the nutrition product
   */
  add(path: "/instance", value: Array<NutritionProductInstance>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instance/${number | "-"}`, value: NutritionProductInstance): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Known or suspected allergens that are a part of this product
   */
  add(path: "/knownAllergen", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/knownAllergen/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Manufacturer, representative or officially responsible for the product
   */
  add(path: "/manufacturer", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/manufacturer/${number | "-"}`,
    value: Reference<Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the product
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The product's nutritional information expressed by the nutrients
   */
  add(path: "/nutrient", value: Array<NutritionProductNutrient>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/nutrient/${number | "-"}`, value: NutritionProductNutrient): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: NutritionProductStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ObservationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Observation";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Fulfills plan, proposal or order
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        | CarePlan
        | DeviceRequest
        | ImmunizationRecommendation
        | MedicationRequest
        | NutritionOrder
        | ServiceRequest
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      | CarePlan
      | DeviceRequest
      | ImmunizationRecommendation
      | MedicationRequest
      | NutritionOrder
      | ServiceRequest
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bodySite", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bodyStructure", value: Reference<BodyStructure>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classification of  type of observation
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Component results
   */
  add(path: "/component", value: Array<ObservationComponent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/component/${number | "-"}`, value: ObservationComponent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dataAbsentReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Related resource from which the observation is made
   */
  add(
    path: "/derivedFrom",
    value: Array<
      Reference<
        | DocumentReference
        | ImagingStudy
        | ImagingSelection
        | QuestionnaireResponse
        | Observation
        | MolecularSequence
        | GenomicStudy
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/derivedFrom/${number | "-"}`,
    value: Reference<
      | DocumentReference
      | ImagingStudy
      | ImagingSelection
      | QuestionnaireResponse
      | Observation
      | MolecularSequence
      | GenomicStudy
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/device", value: Reference<Device | DeviceMetric>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectiveDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectiveTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectiveInstant", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What the observation is about, when it is not about the subject of record
   */
  add(path: "/focus", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/focus/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Related resource that belongs to the Observation group
   */
  add(
    path: "/hasMember",
    value: Array<
      Reference<Observation | QuestionnaireResponse | MolecularSequence>
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/hasMember/${number | "-"}`,
    value: Reference<Observation | QuestionnaireResponse | MolecularSequence>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for observation
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/instantiatesReference",
    value: Reference<ObservationDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * High, low, normal, etc
   */
  add(path: "/interpretation", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/interpretation/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/issued", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/method", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments about the observation
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(
    path: "/partOf",
    value: Array<
      Reference<
        | MedicationAdministration
        | MedicationDispense
        | MedicationStatement
        | Procedure
        | Immunization
        | ImagingStudy
        | GenomicStudy
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<
      | MedicationAdministration
      | MedicationDispense
      | MedicationStatement
      | Procedure
      | Immunization
      | ImagingStudy
      | GenomicStudy
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who is responsible for the observation
   */
  add(
    path: "/performer",
    value: Array<
      Reference<
        | Practitioner
        | PractitionerRole
        | Organization
        | CareTeam
        | Patient
        | RelatedPerson
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | CareTeam
      | Patient
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Provides guide for interpretation
   */
  add(path: "/referenceRange", value: Array<ObservationReferenceRange>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/referenceRange/${number | "-"}`,
    value: ObservationReferenceRange
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/specimen", value: Reference<Specimen | Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ObservationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | Group
      | Device
      | Location
      | Organization
      | Procedure
      | Practitioner
      | Medication
      | Substance
      | BiologicallyDerivedProduct
      | NutritionProduct
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Triggering observation(s)
   */
  add(path: "/triggeredBy", value: Array<ObservationTriggeredBy>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/triggeredBy/${number | "-"}`,
    value: ObservationTriggeredBy
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueQuantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueInteger", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueRatio", value: Ratio): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueSampledData", value: SampledData): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valuePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueAttachment", value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/valueReference", value: Reference<MolecularSequence>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ObservationDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ObservationDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bodySite", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * General type of observation
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Component results
   */
  add(path: "/component", value: Array<ObservationDefinitionComponent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/component/${number | "-"}`,
    value: ObservationDefinitionComponent
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Based on FHIR definition of another observation
   */
  add(path: "/derivedFromCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFromCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Based on external definition
   */
  add(path: "/derivedFromUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFromUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Measurement device or model of device
   */
  add(
    path: "/device",
    value: Array<Reference<DeviceDefinition | Device>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/device/${number | "-"}`,
    value: Reference<DeviceDefinition | Device>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Definitions of related resources belonging to this kind of observation group
   */
  add(
    path: "/hasMember",
    value: Array<Reference<ObservationDefinition | Questionnaire>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/hasMember/${number | "-"}`,
    value: Reference<ObservationDefinition | Questionnaire>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/identifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for this ObservationDefinition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/method", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/multipleResultsAllowed", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/performerType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Quantity | CodeableConcept | string | boolean | integer | Range | Ratio | SampledData | time | dateTime | Period
   */
  add(path: "/permittedDataType", value: Array<ObservationDataType>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/permittedDataType/${number | "-"}`,
    value: ObservationDataType
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unit for quantitative results
   */
  add(path: "/permittedUnit", value: Array<Coding>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/permittedUnit/${number | "-"}`, value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/preferredReportName", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Set of qualified values for observation results
   */
  add(
    path: "/qualifiedValue",
    value: Array<ObservationDefinitionQualifiedValue>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/qualifiedValue/${number | "-"}`,
    value: ObservationDefinitionQualifiedValue
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Kind of specimen used by this type of observation
   */
  add(path: "/specimen", value: Array<Reference<SpecimenDefinition>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/specimen/${number | "-"}`,
    value: Reference<SpecimenDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of subject for the defined observation
   */
  add(path: "/subject", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subject/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Content intends to support these contexts
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface OperationDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "OperationDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/affectsState", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/base", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/comment", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the implementation guide (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/inputProfile", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instance", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for operation definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/kind", value: OperationKind): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outputProfile", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Define overloaded variants for when  generating code
   */
  add(path: "/overload", value: Array<OperationDefinitionOverload>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/overload/${number | "-"}`,
    value: OperationDefinitionOverload
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Parameters for the operation/query
   */
  add(path: "/parameter", value: Array<OperationDefinitionParameter>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/parameter/${number | "-"}`,
    value: OperationDefinitionParameter
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Types this operation applies to
   */
  add(
    path: "/resource",
    value: Array<VersionIndependentResourceTypesAll>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/resource/${number | "-"}`,
    value: VersionIndependentResourceTypesAll
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/system", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface OperationOutcomeFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "OperationOutcome";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A single issue associated with the action
   */
  add(path: "/issue", value: Array<OperationOutcomeIssue>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/issue/${number | "-"}`, value: OperationOutcomeIssue): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface OrganizationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Organization";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A list of alternate names that the organization is known as, or was known as in the past
   */
  add(path: "/alias", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/alias/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Official contact details for the Organization
   */
  add(path: "/contact", value: Array<ExtendedContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ExtendedContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Technical endpoints providing access to services operated for the organization
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifies this organization  across multiple systems
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/partOf", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Qualifications, certifications, accreditations, licenses, training, etc. pertaining to the provision of care
   */
  add(path: "/qualification", value: Array<OrganizationQualification>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/qualification/${number | "-"}`,
    value: OrganizationQualification
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Kind of organization
   */
  add(path: "/type", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/type/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface OrganizationAffiliationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "OrganizationAffiliation";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Definition of the role the participatingOrganization plays
   */
  add(path: "/code", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/code/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Official contact details at the participatingOrganization relevant to this Affiliation
   */
  add(path: "/contact", value: Array<ExtendedContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ExtendedContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Technical endpoints providing access to services operated for this role
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Healthcare services provided through the role
   */
  add(
    path: "/healthcareService",
    value: Array<Reference<HealthcareService>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/healthcareService/${number | "-"}`,
    value: Reference<HealthcareService>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifiers that are specific to this role
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The location(s) at which the role occurs
   */
  add(path: "/location", value: Array<Reference<Location>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/location/${number | "-"}`, value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The network in which the participatingOrganization provides the role's services (if defined) at the indicated locations (if defined)
   */
  add(path: "/network", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/network/${number | "-"}`, value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/organization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/participatingOrganization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific specialty of the participatingOrganization in the context of the role
   */
  add(path: "/specialty", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specialty/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PackagedProductDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "PackagedProductDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information or supporting documentation about the packaged product
   */
  add(
    path: "/attachedDocument",
    value: Array<Reference<DocumentReference>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/attachedDocument/${number | "-"}`,
    value: Reference<DocumentReference>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A total of the complete count of contained items of a particular type/form, independent of sub-packaging or organization. This can be considered as the pack size. See also packaging.containedItem.amount (especially the long definition)
   */
  add(path: "/containedItemQuantity", value: Array<Quantity>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/containedItemQuantity/${number | "-"}`, value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copackagedIndicator", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A unique identifier for this package as whole - not for the content of the package
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The legal status of supply of the packaged item as classified by the regulator
   */
  add(
    path: "/legalStatusOfSupply",
    value: Array<PackagedProductDefinitionLegalStatusOfSupply>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/legalStatusOfSupply/${number | "-"}`,
    value: PackagedProductDefinitionLegalStatusOfSupply
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Manufacturer of this package type (multiple means these are all possible manufacturers)
   */
  add(path: "/manufacturer", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/manufacturer/${number | "-"}`,
    value: Reference<Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Allows specifying that an item is on the market for sale, or that it is not available, and the dates and locations associated
   */
  add(path: "/marketingStatus", value: Array<MarketingStatus>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/marketingStatus/${number | "-"}`, value: MarketingStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The product that this is a pack for
   */
  add(
    path: "/packageFor",
    value: Array<Reference<MedicinalProductDefinition>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/packageFor/${number | "-"}`,
    value: Reference<MedicinalProductDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/packaging", value: PackagedProductDefinitionPackaging): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ParametersFhirPatchBuilder extends ResourceFhirPatchBuilder {
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Operation Parameter
   */
  add(path: "/parameter", value: Array<ParametersParameter>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parameter/${number | "-"}`, value: ParametersParameter): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PatientFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Patient";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An address for the individual
   */
  add(path: "/address", value: Array<Address>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/address/${number | "-"}`, value: Address): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/birthDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A language which may be used to communicate with the patient about his or her health
   */
  add(path: "/communication", value: Array<PatientCommunication>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/communication/${number | "-"}`,
    value: PatientCommunication
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A contact party (e.g. guardian, partner, friend) for the patient
   */
  add(path: "/contact", value: Array<PatientContact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: PatientContact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/gender", value: AdministrativeGender): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient's nominated primary care provider
   */
  add(
    path: "/generalPractitioner",
    value: Array<Reference<Organization | Practitioner | PractitionerRole>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/generalPractitioner/${number | "-"}`,
    value: Reference<Organization | Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An identifier for this patient
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Link to a Patient or RelatedPerson resource that concerns the same actual individual
   */
  add(path: "/link", value: Array<PatientLink>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/link/${number | "-"}`, value: PatientLink): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/managingOrganization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/maritalStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/multipleBirthBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/multipleBirthInteger", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A name associated with the patient
   */
  add(path: "/name", value: Array<HumanName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/name/${number | "-"}`, value: HumanName): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Image of the patient
   */
  add(path: "/photo", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/photo/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A contact detail for the individual
   */
  add(path: "/telecom", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/telecom/${number | "-"}`, value: ContactPoint): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PaymentNoticeFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "PaymentNotice";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/amount", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for the payment notice
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/payee",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/payment", value: Reference<PaymentReconciliation>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/paymentDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/paymentStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recipient", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/reporter",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/request", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/response", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PaymentReconciliationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "PaymentReconciliation";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/accountNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Settlement particulars
   */
  add(path: "/allocation", value: Array<PaymentReconciliationAllocation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/allocation/${number | "-"}`,
    value: PaymentReconciliationAllocation
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/amount", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authorization", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/cardBrand", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/disposition", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/enterer",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expirationDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/formCode", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for a payment reconciliation
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/issuerType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/kind", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/method", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outcome", value: PaymentOutcome): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/paymentIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/paymentIssuer",
    value: Reference<Organization | Patient | RelatedPerson>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Note concerning processing
   */
  add(
    path: "/processNote",
    value: Array<PaymentReconciliationProcessNote>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/processNote/${number | "-"}`,
    value: PaymentReconciliationProcessNote
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/processor", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/referenceNumber", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/request", value: Reference<Task>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requestor",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/returnedAmount", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/tenderedAmount", value: Money): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PermissionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Permission";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/asserter",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | CareTeam
      | Patient
      | RelatedPerson
      | HealthcareService
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/combining", value: PermissionRuleCombining): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The date that permission was asserted
   */
  add(path: "/date", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/date/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/justification", value: PermissionJustification): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Constraints to the Permission
   */
  add(path: "/rule", value: Array<PermissionRule>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/rule/${number | "-"}`, value: PermissionRule): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PermissionStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/validity", value: Period): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PersonFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Person";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * One or more addresses for the person
   */
  add(path: "/address", value: Array<Address>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/address/${number | "-"}`, value: Address): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/birthDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A language which may be used to communicate with the person about his or her health
   */
  add(path: "/communication", value: Array<PersonCommunication>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/communication/${number | "-"}`, value: PersonCommunication): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/gender", value: AdministrativeGender): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A human identifier for this person
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Link to a resource that concerns the same actual person
   */
  add(path: "/link", value: Array<PersonLink>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/link/${number | "-"}`, value: PersonLink): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/managingOrganization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/maritalStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A name associated with the person
   */
  add(path: "/name", value: Array<HumanName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/name/${number | "-"}`, value: HumanName): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Image of the person
   */
  add(path: "/photo", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/photo/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A contact detail for the person
   */
  add(path: "/telecom", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/telecom/${number | "-"}`, value: ContactPoint): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PlanDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "PlanDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Action defined by the plan
   */
  add(path: "/action", value: Array<PlanDefinitionAction>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/action/${number | "-"}`, value: PlanDefinitionAction): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Actors within the plan
   */
  add(path: "/actor", value: Array<PlanDefinitionActor>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/actor/${number | "-"}`, value: PlanDefinitionActor): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/asNeededBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/asNeededCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the content
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the content
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the content
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What the plan is trying to accomplish
   */
  add(path: "/goal", value: Array<PlanDefinitionGoal>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/goal/${number | "-"}`, value: PlanDefinitionGoal): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the plan definition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for plan definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Logic used by the plan definition
   */
  add(path: "/library", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/library/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the content
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subjectReference",
    value: Reference<
      | Group
      | MedicinalProductDefinition
      | SubstanceDefinition
      | AdministrableProductDefinition
      | ManufacturedItemDefinition
      | PackagedProductDefinition
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subtitle", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/usage", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PractitionerFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Practitioner";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Address(es) of the practitioner that are not role specific (typically home address)
   */
  add(path: "/address", value: Array<Address>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/address/${number | "-"}`, value: Address): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/birthDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A language which may be used to communicate with the practitioner
   */
  add(path: "/communication", value: Array<PractitionerCommunication>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/communication/${number | "-"}`,
    value: PractitionerCommunication
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deceasedDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/gender", value: AdministrativeGender): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An identifier for the person as this agent
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The name(s) associated with the practitioner
   */
  add(path: "/name", value: Array<HumanName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/name/${number | "-"}`, value: HumanName): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Image of the person
   */
  add(path: "/photo", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/photo/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Qualifications, certifications, accreditations, licenses, training, etc. pertaining to the provision of care
   */
  add(path: "/qualification", value: Array<PractitionerQualification>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/qualification/${number | "-"}`,
    value: PractitionerQualification
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A contact detail for the practitioner (that apply to all roles)
   */
  add(path: "/telecom", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/telecom/${number | "-"}`, value: ContactPoint): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface PractitionerRoleFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "PractitionerRole";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Times the Practitioner is available at this location and/or healthcare service (including exceptions)
   */
  add(path: "/availability", value: Array<Availability>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/availability/${number | "-"}`, value: Availability): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Collection of characteristics (attributes)
   */
  add(path: "/characteristic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/characteristic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Roles which this practitioner may perform
   */
  add(path: "/code", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/code/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A language the practitioner (in this role) can use in patient communication
   */
  add(path: "/communication", value: Array<AllLanguages>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/communication/${number | "-"}`, value: AllLanguages): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Official contact details relating to this PractitionerRole
   */
  add(path: "/contact", value: Array<ExtendedContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ExtendedContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Endpoints for interacting with the practitioner in this role
   */
  add(path: "/endpoint", value: Array<Reference<Endpoint>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endpoint/${number | "-"}`, value: Reference<Endpoint>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Healthcare services provided for this role's Organization/Location(s)
   */
  add(
    path: "/healthcareService",
    value: Array<Reference<HealthcareService>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/healthcareService/${number | "-"}`,
    value: Reference<HealthcareService>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifiers for a role/location
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Location(s) where the practitioner provides care
   */
  add(path: "/location", value: Array<Reference<Location>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/location/${number | "-"}`, value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/organization", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/practitioner", value: Reference<Practitioner>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific specialty of the practitioner
   */
  add(path: "/specialty", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specialty/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ProcedureFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Procedure";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A request for this procedure
   */
  add(
    path: "/basedOn",
    value: Array<Reference<CarePlan | ServiceRequest>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<CarePlan | ServiceRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Target body sites
   */
  add(path: "/bodySite", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/bodySite/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classification of the procedure
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Complication following the procedure
   */
  add(path: "/complication", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/complication/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Manipulated, implanted, or removed device
   */
  add(path: "/focalDevice", value: Array<ProcedureFocalDevice>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/focalDevice/${number | "-"}`, value: ProcedureFocalDevice): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/focus",
    value: Reference<
      | Patient
      | Group
      | RelatedPerson
      | Practitioner
      | Organization
      | CareTeam
      | PractitionerRole
      | Specimen
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instructions for follow up
   */
  add(path: "/followUp", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/followUp/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Identifiers for this procedure
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information about the procedure
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceAge", value: Age): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/outcome", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(
    path: "/partOf",
    value: Array<Reference<Procedure | Observation | MedicationAdministration>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<Procedure | Observation | MedicationAdministration>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who performed the procedure and what they did
   */
  add(path: "/performer", value: Array<ProcedurePerformer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/performer/${number | "-"}`, value: ProcedurePerformer): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The justification that the procedure was performed
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recorded", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/recorder",
    value: Reference<Patient | RelatedPerson | Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Any report resulting from the procedure
   */
  add(
    path: "/report",
    value: Array<Reference<DiagnosticReport | DocumentReference | Composition>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/report/${number | "-"}`,
    value: Reference<DiagnosticReport | DocumentReference | Composition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reportedBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/reportedReference",
    value: Reference<
      Patient | RelatedPerson | Practitioner | PractitionerRole | Organization
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: EventStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      Patient | Group | Device | Practitioner | Organization | Location
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Extra information relevant to the procedure
   */
  add(path: "/supportingInfo", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supportingInfo/${number | "-"}`,
    value: Reference<Resource>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Items used during procedure
   */
  add(path: "/used", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/used/${number | "-"}`, value: CodeableReference): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ProvenanceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Provenance";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/activity", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Actor involved
   */
  add(path: "/agent", value: Array<ProvenanceAgent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/agent/${number | "-"}`, value: ProvenanceAgent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Authorization (purposeOfUse) related to the event
   */
  add(path: "/authorization", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/authorization/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Workflow authorization within which this event occurred
   */
  add(
    path: "/basedOn",
    value: Array<
      Reference<
        | CarePlan
        | DeviceRequest
        | ImmunizationRecommendation
        | MedicationRequest
        | NutritionOrder
        | ServiceRequest
        | Task
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<
      | CarePlan
      | DeviceRequest
      | ImmunizationRecommendation
      | MedicationRequest
      | NutritionOrder
      | ServiceRequest
      | Task
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An entity used in this activity
   */
  add(path: "/entity", value: Array<ProvenanceEntity>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/entity/${number | "-"}`, value: ProvenanceEntity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurredPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurredDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Policy or plan the activity was defined by
   */
  add(path: "/policy", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/policy/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recorded", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Signature on target
   */
  add(path: "/signature", value: Array<Signature>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/signature/${number | "-"}`, value: Signature): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Target Reference(s) (usually version specific)
   */
  add(path: "/target", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/target/${number | "-"}`, value: Reference<Resource>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface QuestionnaireFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Questionnaire";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Concept that represents the overall questionnaire
   */
  add(path: "/code", value: Array<Coding>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/code/${number | "-"}`, value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Based on Questionnaire
   */
  add(path: "/derivedFrom", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFrom/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for questionnaire
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Questions and sections within the Questionnaire
   */
  add(path: "/item", value: Array<QuestionnaireItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/item/${number | "-"}`, value: QuestionnaireItem): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for questionnaire (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Resource that can be subject of QuestionnaireResponse
   */
  add(path: "/subjectType", value: Array<ResourceType>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subjectType/${number | "-"}`, value: ResourceType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface QuestionnaireResponseFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "QuestionnaireResponse";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/author",
    value: Reference<
      | Device
      | Practitioner
      | PractitionerRole
      | Patient
      | RelatedPerson
      | Organization
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authored", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request fulfilled by this QuestionnaireResponse
   */
  add(
    path: "/basedOn",
    value: Array<Reference<CarePlan | ServiceRequest>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<CarePlan | ServiceRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for this set of answers
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Groups and questions
   */
  add(path: "/item", value: Array<QuestionnaireResponseItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/item/${number | "-"}`, value: QuestionnaireResponseItem): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(path: "/partOf", value: Array<Reference<Observation | Procedure>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<Observation | Procedure>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/questionnaire", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/source",
    value: Reference<
      | Device
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: QuestionnaireResponseStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Resource>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface RegulatedAuthorizationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "RegulatedAuthorization";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional information or supporting documentation about the authorization
   */
  add(
    path: "/attachedDocument",
    value: Array<Reference<DocumentReference>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/attachedDocument/${number | "-"}`,
    value: Reference<DocumentReference>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The legal/regulatory framework or reasons under which this authorization is granted
   */
  add(path: "/basis", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basis/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/case", value: RegulatedAuthorizationCase): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/holder", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for the authorization, typically assigned by the authorizing body
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Condition for which the use of the regulated product applies
   */
  add(path: "/indication", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/indication/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intendedUse", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The territory in which the authorization has been granted
   */
  add(path: "/region", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/region/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/regulator", value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The product type, treatment, facility or activity that is being authorized
   */
  add(
    path: "/subject",
    value: Array<
      Reference<
        | MedicinalProductDefinition
        | BiologicallyDerivedProduct
        | NutritionProduct
        | PackagedProductDefinition
        | ManufacturedItemDefinition
        | Ingredient
        | SubstanceDefinition
        | DeviceDefinition
        | ResearchStudy
        | ActivityDefinition
        | PlanDefinition
        | ObservationDefinition
        | Practitioner
        | Organization
        | Location
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/subject/${number | "-"}`,
    value: Reference<
      | MedicinalProductDefinition
      | BiologicallyDerivedProduct
      | NutritionProduct
      | PackagedProductDefinition
      | ManufacturedItemDefinition
      | Ingredient
      | SubstanceDefinition
      | DeviceDefinition
      | ResearchStudy
      | ActivityDefinition
      | PlanDefinition
      | ObservationDefinition
      | Practitioner
      | Organization
      | Location
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/validityPeriod", value: Period): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface RelatedPersonFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "RelatedPerson";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Address where the related person can be contacted or visited
   */
  add(path: "/address", value: Array<Address>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/address/${number | "-"}`, value: Address): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/birthDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A language which may be used to communicate with the related person about the patient's health
   */
  add(path: "/communication", value: Array<RelatedPersonCommunication>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/communication/${number | "-"}`,
    value: RelatedPersonCommunication
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/gender", value: AdministrativeGender): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A human identifier for this person
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A name associated with the person
   */
  add(path: "/name", value: Array<HumanName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/name/${number | "-"}`, value: HumanName): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Image of the person
   */
  add(path: "/photo", value: Array<Attachment>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/photo/${number | "-"}`, value: Attachment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The relationship of the related person to the patient
   */
  add(path: "/relationship", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relationship/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A contact detail for the person
   */
  add(path: "/telecom", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/telecom/${number | "-"}`, value: ContactPoint): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface RequestOrchestrationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "RequestOrchestration";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Proposed actions, if any
   */
  add(path: "/action", value: Array<RequestOrchestrationAction>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/action/${number | "-"}`, value: RequestOrchestrationAction): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/author",
    value: Reference<Device | Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authoredOn", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Fulfills plan, proposal, or order
   */
  add(path: "/basedOn", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What goals
   */
  add(path: "/goal", value: Array<Reference<Goal>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/goal/${number | "-"}`, value: Reference<Goal>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/groupIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: RequestIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional notes about the response
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why the request orchestration is needed
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request(s) replaced by this request
   */
  add(path: "/replaces", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/replaces/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: RequestStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | CareTeam
      | Device
      | Group
      | HealthcareService
      | Location
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface RequirementsFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Requirements";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Actor for these requirements
   */
  add(path: "/actor", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/actor/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Other set of Requirements this builds on
   */
  add(path: "/derivedFrom", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFrom/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the Requirements (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for Requirements (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External artifact (rule/document etc. that) created this set of requirements
   */
  add(path: "/reference", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reference/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Actual statement as markdown
   */
  add(path: "/statement", value: Array<RequirementsStatement>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/statement/${number | "-"}`, value: RequirementsStatement): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ResearchStudyFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ResearchStudy";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Sponsors, collaborators, and other parties
   */
  add(
    path: "/associatedParty",
    value: Array<ResearchStudyAssociatedParty>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/associatedParty/${number | "-"}`,
    value: ResearchStudyAssociatedParty
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classification for the study
   */
  add(path: "/classifier", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/classifier/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Defined path through the study for a subject
   */
  add(
    path: "/comparisonGroup",
    value: Array<ResearchStudyComparisonGroup>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/comparisonGroup/${number | "-"}`,
    value: ResearchStudyComparisonGroup
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Condition being studied
   */
  add(path: "/condition", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/condition/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/descriptionSummary", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Drugs, devices, etc. under study
   */
  add(path: "/focus", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/focus/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for study
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Used to search for the study
   */
  add(path: "/keyword", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/keyword/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional names for the study
   */
  add(path: "/label", value: Array<ResearchStudyLabel>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/label/${number | "-"}`, value: ResearchStudyLabel): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the study
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A goal for the study
   */
  add(path: "/objective", value: Array<ResearchStudyObjective>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/objective/${number | "-"}`, value: ResearchStudyObjective): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A variable measured during the study
   */
  add(path: "/outcomeMeasure", value: Array<ResearchStudyOutcomeMeasure>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/outcomeMeasure/${number | "-"}`,
    value: ResearchStudyOutcomeMeasure
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of larger study
   */
  add(path: "/partOf", value: Array<Reference<ResearchStudy>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: Reference<ResearchStudy>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/phase", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/primaryPurposeType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Status of study with time for that status
   */
  add(path: "/progressStatus", value: Array<ResearchStudyProgressStatus>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/progressStatus/${number | "-"}`,
    value: ResearchStudyProgressStatus
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Steps followed in executing study
   */
  add(path: "/protocol", value: Array<Reference<PlanDefinition>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/protocol/${number | "-"}`,
    value: Reference<PlanDefinition>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/recruitment", value: ResearchStudyRecruitment): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Geographic area for the study
   */
  add(path: "/region", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/region/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * References, URLs, and attachments
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Link to results generated during the study
   */
  add(
    path: "/result",
    value: Array<Reference<EvidenceReport | Citation | DiagnosticReport>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/result/${number | "-"}`,
    value: Reference<EvidenceReport | Citation | DiagnosticReport>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Facility where study activities are conducted
   */
  add(
    path: "/site",
    value: Array<Reference<Location | ResearchStudy | Organization>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/site/${number | "-"}`,
    value: Reference<Location | ResearchStudy | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classifications of the study design characteristics
   */
  add(path: "/studyDesign", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/studyDesign/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/whyStopped", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ResearchSubjectFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ResearchSubject";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/actualComparisonGroup", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/assignedComparisonGroup", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Agreement to participate in study
   */
  add(path: "/consent", value: Array<Reference<Consent>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/consent/${number | "-"}`, value: Reference<Consent>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for research subject in a study
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/period", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Subject status
   */
  add(path: "/progress", value: Array<ResearchSubjectProgress>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/progress/${number | "-"}`, value: ResearchSubjectProgress): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/study", value: Reference<ResearchStudy>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | Group
      | Specimen
      | Device
      | Medication
      | Substance
      | BiologicallyDerivedProduct
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface RiskAssessmentFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "RiskAssessment";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/basedOn", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information used in assessment
   */
  add(path: "/basis", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basis/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/condition", value: Reference<Condition>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique identifier for the assessment
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/method", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/mitigation", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments on the risk assessment
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/parent", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/performer",
    value: Reference<
      Patient | Practitioner | PractitionerRole | RelatedPerson | Device
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Outcome predicted
   */
  add(path: "/prediction", value: Array<RiskAssessmentPrediction>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/prediction/${number | "-"}`,
    value: RiskAssessmentPrediction
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why the assessment was necessary?
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: ObservationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subject", value: Reference<Patient | Group>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ScheduleFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Schedule";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/active", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Resource(s) that availability information is being provided for
   */
  add(
    path: "/actor",
    value: Array<
      Reference<
        | Patient
        | Practitioner
        | PractitionerRole
        | CareTeam
        | RelatedPerson
        | Device
        | HealthcareService
        | Location
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/actor/${number | "-"}`,
    value: Reference<
      | Patient
      | Practitioner
      | PractitionerRole
      | CareTeam
      | RelatedPerson
      | Device
      | HealthcareService
      | Location
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/comment", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Ids for this item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/planningHorizon", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * High-level category
   */
  add(path: "/serviceCategory", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/serviceCategory/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specific service
   */
  add(path: "/serviceType", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/serviceType/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Type of specialty needed
   */
  add(path: "/specialty", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specialty/${number | "-"}`, value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SearchParameterFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SearchParameter";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The resource type(s) this search parameter applies to
   */
  add(path: "/base", value: Array<VersionIndependentResourceTypesAll>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/base/${number | "-"}`,
    value: VersionIndependentResourceTypesAll
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Chained names supported
   */
  add(path: "/chain", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/chain/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * eq | ne | gt | lt | ge | le | sa | eb | ap
   */
  add(path: "/comparator", value: Array<SearchComparator>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/comparator/${number | "-"}`, value: SearchComparator): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * For Composite resources to define the parts
   */
  add(path: "/component", value: Array<SearchParameterComponent>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/component/${number | "-"}`,
    value: SearchParameterComponent
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/constraint", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/derivedFrom", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expression", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the search parameter (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for search parameter (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * missing | exact | contains | not | text | in | not-in | below | above | type | identifier | of-type | code-text | text-advanced | iterate
   */
  add(path: "/modifier", value: Array<SearchModifierCode>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/modifier/${number | "-"}`, value: SearchModifierCode): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/multipleAnd", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/multipleOr", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/processingMode", value: SearchProcessingModeType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Types of resource (if a resource reference)
   */
  add(path: "/target", value: Array<VersionIndependentResourceTypesAll>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/target/${number | "-"}`,
    value: VersionIndependentResourceTypesAll
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: SearchParamType): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ServiceRequestFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ServiceRequest";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/asNeededBoolean", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/asNeededCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authoredOn", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What request fulfills
   */
  add(
    path: "/basedOn",
    value: Array<Reference<CarePlan | ServiceRequest | MedicationRequest>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/basedOn/${number | "-"}`,
    value: Reference<CarePlan | ServiceRequest | MedicationRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Coded location on Body
   */
  add(path: "/bodySite", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/bodySite/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/bodyStructure", value: Reference<BodyStructure>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Classification of service
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doNotPerform", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What the service request is about, when it is not about the subject of record
   */
  add(path: "/focus", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/focus/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifiers assigned to this order
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates FHIR protocol or definition
   */
  add(path: "/instantiatesCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Instantiates external protocol or definition
   */
  add(path: "/instantiatesUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/instantiatesUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Associated insurance coverage
   */
  add(
    path: "/insurance",
    value: Array<Reference<Coverage | ClaimResponse>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/insurance/${number | "-"}`,
    value: Reference<Coverage | ClaimResponse>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: RequestIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Requested location
   */
  add(path: "/location", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/location/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional order information
   */
  add(path: "/orderDetail", value: Array<ServiceRequestOrderDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/orderDetail/${number | "-"}`,
    value: ServiceRequestOrderDetail
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient or consumer-oriented instructions
   */
  add(
    path: "/patientInstruction",
    value: Array<ServiceRequestPatientInstruction>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/patientInstruction/${number | "-"}`,
    value: ServiceRequestPatientInstruction
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Requested performer
   */
  add(
    path: "/performer",
    value: Array<
      Reference<
        | Practitioner
        | PractitionerRole
        | Organization
        | CareTeam
        | HealthcareService
        | Patient
        | Device
        | RelatedPerson
      >
    >
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/performer/${number | "-"}`,
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | CareTeam
      | HealthcareService
      | Patient
      | Device
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/performerType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantityQuantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantityRatio", value: Ratio): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantityRange", value: Range): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Explanation/Justification for procedure or service
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request provenance
   */
  add(path: "/relevantHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relevantHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What request replaces
   */
  add(path: "/replaces", value: Array<Reference<ServiceRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/replaces/${number | "-"}`,
    value: Reference<ServiceRequest>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requester",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | Patient
      | RelatedPerson
      | Device
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/requisition", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Procedure Samples
   */
  add(path: "/specimen", value: Array<Reference<Specimen>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specimen/${number | "-"}`, value: Reference<Specimen>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: RequestStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<Patient | Group | Location | Device>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional clinical information
   */
  add(path: "/supportingInfo", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/supportingInfo/${number | "-"}`, value: CodeableReference): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SlotFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Slot";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The style of appointment or patient that may be booked in the slot (not service type)
   */
  add(path: "/appointmentType", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/appointmentType/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/comment", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/end", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Ids for this item
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/overbooked", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/schedule", value: Reference<Schedule>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A broad categorization of the service that is to be performed during this appointment
   */
  add(path: "/serviceCategory", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/serviceCategory/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The type of appointments that can be booked into this slot (ideally this would be an identifiable service - which is at a location, rather than the location itself). If provided then this overrides the value provided on the Schedule resource
   */
  add(path: "/serviceType", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/serviceType/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The specialty of a practitioner that would be required to perform the service requested in this appointment
   */
  add(path: "/specialty", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/specialty/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/start", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: SlotStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SpecimenFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Specimen";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/accessionIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/collection", value: SpecimenCollection): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/combined", value: SpecimenCombined): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * State of the specimen
   */
  add(path: "/condition", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/condition/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Direct container of specimen (tube/slide, etc.)
   */
  add(path: "/container", value: Array<SpecimenContainer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/container/${number | "-"}`, value: SpecimenContainer): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The physical feature of a specimen
   */
  add(path: "/feature", value: Array<SpecimenFeature>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/feature/${number | "-"}`, value: SpecimenFeature): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External Identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specimen from which this specimen originated
   */
  add(path: "/parent", value: Array<Reference<Specimen>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parent/${number | "-"}`, value: Reference<Specimen>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Processing and processing step details
   */
  add(path: "/processing", value: Array<SpecimenProcessing>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/processing/${number | "-"}`, value: SpecimenProcessing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/receivedTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why the specimen was collected
   */
  add(path: "/request", value: Array<Reference<ServiceRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/request/${number | "-"}`, value: Reference<ServiceRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The role the specimen serves
   */
  add(path: "/role", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/role/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: SpecimenStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/subject",
    value: Reference<
      | Patient
      | Group
      | Device
      | BiologicallyDerivedProduct
      | Substance
      | Location
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SpecimenDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SpecimenDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specimen collection procedure
   */
  add(path: "/collection", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/collection/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Based on FHIR definition of another SpecimenDefinition
   */
  add(path: "/derivedFromCanonical", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFromCanonical/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Based on external definition
   */
  add(path: "/derivedFromUri", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFromUri/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/identifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for this SpecimenDefinition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Patient preparation for collection
   */
  add(path: "/patientPreparation", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/patientPreparation/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectCodeableConcept", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subjectReference", value: Reference<Group>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timeAspect", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/typeCollected", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specimen in container intended for testing by lab
   */
  add(path: "/typeTested", value: Array<SpecimenDefinitionTypeTested>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/typeTested/${number | "-"}`,
    value: SpecimenDefinitionTypeTested
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Content intends to support these contexts
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface StructureDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "StructureDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/abstract", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/baseDefinition", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * If an extension, where it can be used in instances
   */
  add(path: "/context", value: Array<StructureDefinitionContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/context/${number | "-"}`,
    value: StructureDefinitionContext
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * FHIRPath invariants - when the extension can be used
   */
  add(path: "/contextInvariant", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contextInvariant/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/derivation", value: TypeDerivationRule): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/differential", value: StructureDefinitionDifferential): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/fhirVersion", value: FHIRVersion): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the structure definition
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for structure definition (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Assist with indexing and finding
   */
  add(path: "/keyword", value: Array<Coding>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/keyword/${number | "-"}`, value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/kind", value: StructureDefinitionKind): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External specification that the content is mapped to
   */
  add(path: "/mapping", value: Array<StructureDefinitionMapping>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/mapping/${number | "-"}`,
    value: StructureDefinitionMapping
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/snapshot", value: StructureDefinitionSnapshot): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface StructureMapFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "StructureMap";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Definition of the constant value used in the map rules
   */
  add(path: "/const", value: Array<StructureMapConst>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/const/${number | "-"}`, value: StructureMapConst): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Named sections for reader convenience
   */
  add(path: "/group", value: Array<StructureMapGroup>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/group/${number | "-"}`, value: StructureMapGroup): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the structure map
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Other maps used by this map (canonical URLs)
   */
  add(path: "/import", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/import/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for structure map (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Structure Definition used by this map
   */
  add(path: "/structure", value: Array<StructureMapStructure>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/structure/${number | "-"}`, value: StructureMapStructure): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubscriptionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Subscription";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/channelType", value: Coding): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for source (e.g. troubleshooting)
   */
  add(path: "/contact", value: Array<ContactPoint>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactPoint): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/content", value: SubscriptionPayloadContent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/contentType", value: MimeTypes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/end", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/endpoint", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Criteria for narrowing the subscription topic stream
   */
  add(path: "/filterBy", value: Array<SubscriptionFilterBy>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/filterBy/${number | "-"}`, value: SubscriptionFilterBy): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/heartbeatPeriod", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifiers (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/managingEntity",
    value: Reference<
      | CareTeam
      | HealthcareService
      | Organization
      | RelatedPerson
      | Patient
      | Practitioner
      | PractitionerRole
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/maxCount", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Channel type
   */
  add(path: "/parameter", value: Array<SubscriptionParameter>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parameter/${number | "-"}`, value: SubscriptionParameter): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reason", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: SubscriptionStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/timeout", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/topic", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubscriptionStatusFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SubscriptionStatus";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * List of errors on the subscription
   */
  add(path: "/error", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/error/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/eventsSinceSubscriptionStart", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Detailed information about any events relevant to this notification
   */
  add(
    path: "/notificationEvent",
    value: Array<SubscriptionStatusNotificationEvent>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/notificationEvent/${number | "-"}`,
    value: SubscriptionStatusNotificationEvent
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: SubscriptionStatusCodes): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/subscription", value: Reference<Subscription>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/topic", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: SubscriptionNotificationType): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubscriptionTopicFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SubscriptionTopic";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Properties by which a Subscription can filter notifications from the SubscriptionTopic
   */
  add(path: "/canFilterBy", value: Array<SubscriptionTopicCanFilterBy>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/canFilterBy/${number | "-"}`,
    value: SubscriptionTopicCanFilterBy
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Based on FHIR protocol or definition
   */
  add(path: "/derivedFrom", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/derivedFrom/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Event definitions the SubscriptionTopic
   */
  add(path: "/eventTrigger", value: Array<SubscriptionTopicEventTrigger>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/eventTrigger/${number | "-"}`,
    value: SubscriptionTopicEventTrigger
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier for subscription topic
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction of the SubscriptionTopic (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Properties for describing the shape of notifications generated by this topic
   */
  add(
    path: "/notificationShape",
    value: Array<SubscriptionTopicNotificationShape>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/notificationShape/${number | "-"}`,
    value: SubscriptionTopicNotificationShape
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Definition of a resource-based trigger for the subscription topic
   */
  add(
    path: "/resourceTrigger",
    value: Array<SubscriptionTopicResourceTrigger>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/resourceTrigger/${number | "-"}`,
    value: SubscriptionTopicResourceTrigger
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Content intends to support these contexts
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubstanceFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Substance";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What class/type of substance this is
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expiry", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Unique identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Composition information about the substance
   */
  add(path: "/ingredient", value: Array<SubstanceIngredient>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/ingredient/${number | "-"}`, value: SubstanceIngredient): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instance", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FHIRSubstanceStatus): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubstanceDefinitionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SubstanceDefinition";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * General specifications for this substance
   */
  add(
    path: "/characterization",
    value: Array<SubstanceDefinitionCharacterization>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/characterization/${number | "-"}`,
    value: SubstanceDefinitionCharacterization
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A categorization, high level e.g. polymer or nucleic acid, or food, chemical, biological, or lower e.g. polymer linear or branch chain, or type of impurity
   */
  add(path: "/classification", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/classification/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Codes associated with the substance
   */
  add(path: "/code", value: Array<SubstanceDefinitionCode>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/code/${number | "-"}`, value: SubstanceDefinitionCode): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/domain", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The quality standard, established benchmark, to which substance complies (e.g. USP/NF, BP)
   */
  add(path: "/grade", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/grade/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Identifier by which this substance is known
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Supporting literature
   */
  add(path: "/informationSource", value: Array<Reference<Citation>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/informationSource/${number | "-"}`,
    value: Reference<Citation>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The entity that creates, makes, produces or fabricates the substance
   */
  add(path: "/manufacturer", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/manufacturer/${number | "-"}`,
    value: Reference<Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Moiety, for structural modifications
   */
  add(path: "/moiety", value: Array<SubstanceDefinitionMoiety>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/moiety/${number | "-"}`, value: SubstanceDefinitionMoiety): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The average mass of a molecule of a compound
   */
  add(
    path: "/molecularWeight",
    value: Array<SubstanceDefinitionMolecularWeight>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/molecularWeight/${number | "-"}`,
    value: SubstanceDefinitionMolecularWeight
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Names applicable to this substance
   */
  add(path: "/name", value: Array<SubstanceDefinitionName>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/name/${number | "-"}`, value: SubstanceDefinitionName): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Textual comment about the substance's catalogue or registry record
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/nucleicAcid", value: Reference<SubstanceNucleicAcid>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/polymer", value: Reference<SubstancePolymer>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * General specifications for this substance
   */
  add(path: "/property", value: Array<SubstanceDefinitionProperty>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/property/${number | "-"}`,
    value: SubstanceDefinitionProperty
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/protein", value: Reference<SubstanceProtein>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/referenceInformation",
    value: Reference<SubstanceReferenceInformation>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A link between this substance and another
   */
  add(
    path: "/relationship",
    value: Array<SubstanceDefinitionRelationship>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relationship/${number | "-"}`,
    value: SubstanceDefinitionRelationship
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sourceMaterial", value: SubstanceDefinitionSourceMaterial): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/structure", value: SubstanceDefinitionStructure): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An entity that is the source for the substance. It may be different from the manufacturer
   */
  add(path: "/supplier", value: Array<Reference<Organization>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/supplier/${number | "-"}`, value: Reference<Organization>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubstanceNucleicAcidFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SubstanceNucleicAcid";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/areaOfHybridisation", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/numberOfSubunits", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/oligoNucleotideType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sequenceType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Subunits are listed in order of decreasing length; sequences of the same length will be ordered by molecular weight; subunits that have identical sequences will be repeated multiple times
   */
  add(path: "/subunit", value: Array<SubstanceNucleicAcidSubunit>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/subunit/${number | "-"}`,
    value: SubstanceNucleicAcidSubunit
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubstancePolymerFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SubstancePolymer";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/class", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Descrtibes the copolymer sequence type (polymer connectivity)
   */
  add(path: "/copolymerConnectivity", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/copolymerConnectivity/${number | "-"}`,
    value: CodeableConcept
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/geometry", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/identifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/modification", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Todo
   */
  add(path: "/monomerSet", value: Array<SubstancePolymerMonomerSet>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/monomerSet/${number | "-"}`,
    value: SubstancePolymerMonomerSet
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Specifies and quantifies the repeated units and their configuration
   */
  add(path: "/repeat", value: Array<SubstancePolymerRepeat>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/repeat/${number | "-"}`, value: SubstancePolymerRepeat): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubstanceProteinFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SubstanceProtein";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The disulphide bond between two cysteine residues either on the same subunit or on two different subunits shall be described. The position of the disulfide bonds in the SubstanceProtein shall be listed in increasing order of subunit number and position within subunit followed by the abbreviation of the amino acids involved. The disulfide linkage positions shall actually contain the amino acid Cysteine at the respective positions
   */
  add(path: "/disulfideLinkage", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/disulfideLinkage/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/numberOfSubunits", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sequenceType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * This subclause refers to the description of each subunit constituting the SubstanceProtein. A subunit is a linear sequence of amino acids linked through peptide bonds. The Subunit information shall be provided when the finished SubstanceProtein is a complex of multiple sequences; subunits are not used to delineate domains within a single sequence. Subunits are listed in order of decreasing length; sequences of the same length will be ordered by decreasing molecular weight; subunits that have identical sequences will be repeated multiple times
   */
  add(path: "/subunit", value: Array<SubstanceProteinSubunit>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/subunit/${number | "-"}`, value: SubstanceProteinSubunit): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubstanceReferenceInformationFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SubstanceReferenceInformation";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/comment", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Todo
   */
  add(path: "/gene", value: Array<SubstanceReferenceInformationGene>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/gene/${number | "-"}`,
    value: SubstanceReferenceInformationGene
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Todo
   */
  add(
    path: "/geneElement",
    value: Array<SubstanceReferenceInformationGeneElement>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/geneElement/${number | "-"}`,
    value: SubstanceReferenceInformationGeneElement
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Todo
   */
  add(path: "/target", value: Array<SubstanceReferenceInformationTarget>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/target/${number | "-"}`,
    value: SubstanceReferenceInformationTarget
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SubstanceSourceMaterialFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SubstanceSourceMaterial";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The country where the plant material is harvested or the countries where the plasma is sourced from as laid down in accordance with the Plasma Master File. For “Plasma-derived substances” the attribute country of origin provides information about the countries used for the manufacturing of the Cryopoor plama or Crioprecipitate
   */
  add(path: "/countryOfOrigin", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/countryOfOrigin/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/developmentStage", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Many complex materials are fractions of parts of plants, animals, or minerals. Fraction elements are often necessary to define both Substances and Specified Group 1 Substances. For substances derived from Plants, fraction information will be captured at the Substance information level ( . Oils, Juices and Exudates). Additional information for Extracts, such as extraction solvent composition, will be captured at the Specified Substance Group 1 information level. For plasma-derived products fraction information will be captured at the Substance and the Specified Substance Group 1 levels
   */
  add(
    path: "/fractionDescription",
    value: Array<SubstanceSourceMaterialFractionDescription>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/fractionDescription/${number | "-"}`,
    value: SubstanceSourceMaterialFractionDescription
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The place/region where the plant is harvested or the places/regions where the animal source material has its habitat
   */
  add(path: "/geographicalLocation", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/geographicalLocation/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/organism", value: SubstanceSourceMaterialOrganism): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/organismId", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/organismName", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The parent of the herbal drug Ginkgo biloba, Leaf is the substance ID of the substance (fresh) of Ginkgo biloba L. or Ginkgo biloba L. (Whole plant)
   */
  add(path: "/parentSubstanceId", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parentSubstanceId/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The parent substance of the Herbal Drug, or Herbal preparation
   */
  add(path: "/parentSubstanceName", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parentSubstanceName/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * To do
   */
  add(
    path: "/partDescription",
    value: Array<SubstanceSourceMaterialPartDescription>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partDescription/${number | "-"}`,
    value: SubstanceSourceMaterialPartDescription
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sourceMaterialClass", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sourceMaterialState", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/sourceMaterialType", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SupplyDeliveryFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SupplyDelivery";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Fulfills plan, proposal or order
   */
  add(path: "/basedOn", value: Array<Reference<SupplyRequest>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<SupplyRequest>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/destination", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(
    path: "/partOf",
    value: Array<Reference<SupplyDelivery | Contract>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/partOf/${number | "-"}`,
    value: Reference<SupplyDelivery | Contract>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who received the delivery
   */
  add(
    path: "/receiver",
    value: Array<Reference<Practitioner | PractitionerRole | Organization>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/receiver/${number | "-"}`,
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: SupplyDeliveryStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The item that is delivered or supplied
   */
  add(path: "/suppliedItem", value: Array<SupplyDeliverySuppliedItem>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/suppliedItem/${number | "-"}`,
    value: SupplyDeliverySuppliedItem
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/supplier",
    value: Reference<Practitioner | PractitionerRole | Organization>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/type", value: SupplyDeliverySupplyItemType): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface SupplyRequestFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "SupplyRequest";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authoredOn", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What other request is fulfilled by this supply request
   */
  add(path: "/basedOn", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/category", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deliverFor", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/deliverFrom", value: Reference<Organization | Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/deliverTo",
    value: Reference<Organization | Location | Patient | RelatedPerson>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for SupplyRequest
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/item", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceDateTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrencePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/occurrenceTiming", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Ordered item details
   */
  add(path: "/parameter", value: Array<SupplyRequestParameter>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/parameter/${number | "-"}`, value: SupplyRequestParameter): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/quantity", value: Quantity): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The reason why the supply item was requested
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requester",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | Patient
      | RelatedPerson
      | Device
      | CareTeam
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: SupplyRequestStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who is intended to fulfill the request
   */
  add(
    path: "/supplier",
    value: Array<Reference<Organization | HealthcareService>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/supplier/${number | "-"}`,
    value: Reference<Organization | HealthcareService>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface TaskFhirPatchBuilder extends DomainResourceFhirPatchBuilder {
  resourceType: "Task";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authoredOn", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request fulfilled by this task
   */
  add(path: "/basedOn", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/businessStatus", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/doNotPerform", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/executionPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/focus", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/for", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/groupIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Task Instance Identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information used to perform task
   */
  add(path: "/input", value: Array<TaskInput>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/input/${number | "-"}`, value: TaskInput): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Associated insurance coverage
   */
  add(
    path: "/insurance",
    value: Array<Reference<Coverage | ClaimResponse>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/insurance/${number | "-"}`,
    value: Reference<Coverage | ClaimResponse>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: TaskIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastModified", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the task
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information produced as part of task
   */
  add(path: "/output", value: Array<TaskOutput>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/output/${number | "-"}`, value: TaskOutput): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/owner",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | CareTeam
      | Patient
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Composite task
   */
  add(path: "/partOf", value: Array<Reference<Task>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: Reference<Task>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who or what performed the task
   */
  add(path: "/performer", value: Array<TaskPerformer>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/performer/${number | "-"}`, value: TaskPerformer): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Why task is needed
   */
  add(path: "/reason", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reason/${number | "-"}`, value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Key events in history of the Task
   */
  add(path: "/relevantHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relevantHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who should perform Task
   */
  add(path: "/requestedPerformer", value: Array<CodeableReference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/requestedPerformer/${number | "-"}`,
    value: CodeableReference
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/requestedPeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requester",
    value: Reference<
      | Device
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/restriction", value: TaskRestriction): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: TaskStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableReference): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface TerminologyCapabilitiesFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "TerminologyCapabilities";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/closure", value: TerminologyCapabilitiesClosure): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/codeSearch", value: CodeSearchSupport): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A code system supported by the server
   */
  add(
    path: "/codeSystem",
    value: Array<TerminologyCapabilitiesCodeSystem>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/codeSystem/${number | "-"}`,
    value: TerminologyCapabilitiesCodeSystem
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expansion", value: TerminologyCapabilitiesExpansion): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the terminology capabilities
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/implementation",
    value: TerminologyCapabilitiesImplementation
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for terminology capabilities (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/kind", value: CapabilityStatementKind): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lockedDate", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/software", value: TerminologyCapabilitiesSoftware): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/translation", value: TerminologyCapabilitiesTranslation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/validateCode", value: TerminologyCapabilitiesValidateCode): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface TestPlanFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "TestPlan";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The category of the Test Plan - can be acceptance, unit, performance
   */
  add(path: "/category", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/category/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The required criteria to execute the test plan - e.g. preconditions, previous tests
   */
  add(path: "/dependency", value: Array<TestPlanDependency>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/dependency/${number | "-"}`, value: TestPlanDependency): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/exitCriteria", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business identifier identifier for the test plan
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction where the test plan applies (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * What is being tested with this Test Plan - a conformance resource, or narrative criteria, or an external reference
   */
  add(path: "/scope", value: Array<Reference>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/scope/${number | "-"}`, value: Reference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The test cases that constitute this plan
   */
  add(path: "/testCase", value: Array<TestPlanTestCase>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/testCase/${number | "-"}`, value: TestPlanTestCase): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/testTools", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface TestReportFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "TestReport";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/identifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/issued", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A participant in the test execution, either the execution engine, a client, or a server
   */
  add(path: "/participant", value: Array<TestReportParticipant>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/participant/${number | "-"}`, value: TestReportParticipant): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/result", value: TestReportResult): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/score", value: number): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/setup", value: TestReportSetup): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: TestReportStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/teardown", value: TestReportTeardown): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A test executed from the test script
   */
  add(path: "/test", value: Array<TestReportTest>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/test/${number | "-"}`, value: TestReportTest): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/tester", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/testScript", value: string): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface TestScriptFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "TestScript";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An abstract server representing a destination or receiver in a message exchange
   */
  add(path: "/destination", value: Array<TestScriptDestination>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/destination/${number | "-"}`, value: TestScriptDestination): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Fixture in the test script - by reference (uri)
   */
  add(path: "/fixture", value: Array<TestScriptFixture>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/fixture/${number | "-"}`, value: TestScriptFixture): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the test script
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for test script (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/metadata", value: TestScriptMetadata): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * An abstract server representing a client or sender in a message exchange
   */
  add(path: "/origin", value: Array<TestScriptOrigin>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/origin/${number | "-"}`, value: TestScriptOrigin): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Reference of the validation profile
   */
  add(path: "/profile", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/profile/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Indication of the artifact(s) that are tested by this test case
   */
  add(path: "/scope", value: Array<TestScriptScope>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/scope/${number | "-"}`, value: TestScriptScope): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/setup", value: TestScriptSetup): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/teardown", value: TestScriptTeardown): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A test in this script
   */
  add(path: "/test", value: Array<TestScriptTest>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/test/${number | "-"}`, value: TestScriptTest): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Placeholder for evaluated elements
   */
  add(path: "/variable", value: Array<TestScriptVariable>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/variable/${number | "-"}`, value: TestScriptVariable): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface TransportFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "Transport";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/authoredOn", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Request fulfilled by this transport
   */
  add(path: "/basedOn", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/basedOn/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/code", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/completionTime", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/currentLocation", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/focus", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/for", value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/groupIdentifier", value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/history", value: Reference<Transport>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * External identifier
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information used to perform transport
   */
  add(path: "/input", value: Array<TransportInput>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/input/${number | "-"}`, value: TransportInput): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesCanonical", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/instantiatesUri", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Associated insurance coverage
   */
  add(
    path: "/insurance",
    value: Array<Reference<Coverage | ClaimResponse>>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/insurance/${number | "-"}`,
    value: Reference<Coverage | ClaimResponse>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/intent", value: TransportIntent): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastModified", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/location", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Comments made about the transport
   */
  add(path: "/note", value: Array<Annotation>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/note/${number | "-"}`, value: Annotation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information produced as part of transport
   */
  add(path: "/output", value: Array<TransportOutput>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/output/${number | "-"}`, value: TransportOutput): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/owner",
    value: Reference<
      | Practitioner
      | PractitionerRole
      | Organization
      | CareTeam
      | HealthcareService
      | Patient
      | Device
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Part of referenced event
   */
  add(path: "/partOf", value: Array<Reference<Transport>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/partOf/${number | "-"}`, value: Reference<Transport>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Requested performer
   */
  add(path: "/performerType", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/performerType/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/priority", value: RequestPriority): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/reason", value: CodeableReference): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Key events in history of the Transport
   */
  add(path: "/relevantHistory", value: Array<Reference<Provenance>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/relevantHistory/${number | "-"}`,
    value: Reference<Provenance>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/requestedLocation", value: Reference<Location>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/requester",
    value: Reference<
      | Device
      | Organization
      | Patient
      | Practitioner
      | PractitionerRole
      | RelatedPerson
    >
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/restriction", value: TransportRestriction): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: TransportStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusReason", value: CodeableConcept): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface ValueSetFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "ValueSet";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/approvalDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who authored the ValueSet
   */
  add(path: "/author", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/author/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/compose", value: ValueSetCompose): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Contact details for the publisher
   */
  add(path: "/contact", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/contact/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyright", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/copyrightLabel", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/date", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/description", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who edited the ValueSet
   */
  add(path: "/editor", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/editor/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/effectivePeriod", value: Period): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who endorsed the ValueSet
   */
  add(path: "/endorser", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/endorser/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/expansion", value: ValueSetExpansion): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/experimental", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional identifier for the value set (business identifier)
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/immutable", value: boolean): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Intended jurisdiction for value set (if applicable)
   */
  add(path: "/jurisdiction", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/jurisdiction/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastReviewDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/name", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/publisher", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/purpose", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Additional documentation, citations, etc
   */
  add(path: "/relatedArtifact", value: Array<RelatedArtifact>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/relatedArtifact/${number | "-"}`, value: RelatedArtifact): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Who reviewed the ValueSet
   */
  add(path: "/reviewer", value: Array<ContactDetail>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/reviewer/${number | "-"}`, value: ContactDetail): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/scope", value: ValueSetScope): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: PublicationStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/title", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * E.g. Education, Treatment, Assessment, etc
   */
  add(path: "/topic", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/topic/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/url", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The context that the content is intended to support
   */
  add(path: "/useContext", value: Array<UsageContext>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/useContext/${number | "-"}`, value: UsageContext): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/version", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmString", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/versionAlgorithmCoding", value: Coding): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface VerificationResultFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "VerificationResult";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/attestation", value: VerificationResultAttestation): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/failureAction", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/frequency", value: Timing): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/lastPerformed", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/need", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/nextScheduled", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information about the primary source(s) involved in validation
   */
  add(
    path: "/primarySource",
    value: Array<VerificationResultPrimarySource>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/primarySource/${number | "-"}`,
    value: VerificationResultPrimarySource
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: VerificationResultStatus): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/statusDate", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * A resource that was validated
   */
  add(path: "/target", value: Array<Reference<Resource>>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/target/${number | "-"}`, value: Reference<Resource>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The fhirpath location(s) within the resource that was validated
   */
  add(path: "/targetLocation", value: Array<string>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/targetLocation/${number | "-"}`, value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * The primary process by which the target is validated (edit check; value set; primary source; multiple sources; standalone; in context)
   */
  add(path: "/validationProcess", value: Array<CodeableConcept>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/validationProcess/${number | "-"}`, value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/validationType", value: CodeableConcept): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Information about the entity validating information
   */
  add(path: "/validator", value: Array<VerificationResultValidator>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/validator/${number | "-"}`,
    value: VerificationResultValidator
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}
export interface VisionPrescriptionFhirPatchBuilder
  extends DomainResourceFhirPatchBuilder {
  resourceType: "VisionPrescription";

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/created", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/dateWritten", value: string): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/encounter", value: Reference<Encounter>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Business Identifier for vision prescription
   */
  add(path: "/identifier", value: Array<Identifier>): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: `/identifier/${number | "-"}`, value: Identifier): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * Vision lens authorization
   */
  add(
    path: "/lensSpecification",
    value: Array<VisionPrescriptionLensSpecification>
  ): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: `/lensSpecification/${number | "-"}`,
    value: VisionPrescriptionLensSpecification
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/patient", value: Reference<Patient>): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(
    path: "/prescriber",
    value: Reference<Practitioner | PractitionerRole>
  ): this;
  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   *
   * {{short}}
   */
  add(path: "/status", value: FinancialResourceStatusCodes): this;

  /**
   * [Adds](https://jsonpatch.com/#add) a value to an object or inserts it into an array.
   * In the case of an array, the value is inserted before the given index.
   * The - character can be used instead of an index to insert at the end of an array.
   */
  add(path: string, value: unknown): this;
}

export type AnyFhirPatchBuilder =
  | AccountFhirPatchBuilder
  | ActivityDefinitionFhirPatchBuilder
  | ActorDefinitionFhirPatchBuilder
  | AdministrableProductDefinitionFhirPatchBuilder
  | AdverseEventFhirPatchBuilder
  | AllergyIntoleranceFhirPatchBuilder
  | AppointmentFhirPatchBuilder
  | AppointmentResponseFhirPatchBuilder
  | ArtifactAssessmentFhirPatchBuilder
  | AuditEventFhirPatchBuilder
  | BasicFhirPatchBuilder
  | BiologicallyDerivedProductFhirPatchBuilder
  | BiologicallyDerivedProductDispenseFhirPatchBuilder
  | BodyStructureFhirPatchBuilder
  | CanonicalResourceFhirPatchBuilder
  | CapabilityStatementFhirPatchBuilder
  | CarePlanFhirPatchBuilder
  | CareTeamFhirPatchBuilder
  | ChargeItemFhirPatchBuilder
  | ChargeItemDefinitionFhirPatchBuilder
  | CitationFhirPatchBuilder
  | ClaimFhirPatchBuilder
  | ClaimResponseFhirPatchBuilder
  | ClinicalImpressionFhirPatchBuilder
  | ClinicalUseDefinitionFhirPatchBuilder
  | CodeSystemFhirPatchBuilder
  | CommunicationFhirPatchBuilder
  | CommunicationRequestFhirPatchBuilder
  | CompartmentDefinitionFhirPatchBuilder
  | CompositionFhirPatchBuilder
  | ConceptMapFhirPatchBuilder
  | ConditionFhirPatchBuilder
  | ConditionDefinitionFhirPatchBuilder
  | ConsentFhirPatchBuilder
  | ContractFhirPatchBuilder
  | CoverageFhirPatchBuilder
  | CoverageEligibilityRequestFhirPatchBuilder
  | CoverageEligibilityResponseFhirPatchBuilder
  | DetectedIssueFhirPatchBuilder
  | DeviceFhirPatchBuilder
  | DeviceAssociationFhirPatchBuilder
  | DeviceDefinitionFhirPatchBuilder
  | DeviceDispenseFhirPatchBuilder
  | DeviceMetricFhirPatchBuilder
  | DeviceRequestFhirPatchBuilder
  | DeviceUsageFhirPatchBuilder
  | DiagnosticReportFhirPatchBuilder
  | DocumentReferenceFhirPatchBuilder
  | EncounterFhirPatchBuilder
  | EncounterHistoryFhirPatchBuilder
  | EndpointFhirPatchBuilder
  | EnrollmentRequestFhirPatchBuilder
  | EnrollmentResponseFhirPatchBuilder
  | EpisodeOfCareFhirPatchBuilder
  | EventDefinitionFhirPatchBuilder
  | EvidenceFhirPatchBuilder
  | EvidenceReportFhirPatchBuilder
  | EvidenceVariableFhirPatchBuilder
  | ExampleScenarioFhirPatchBuilder
  | ExplanationOfBenefitFhirPatchBuilder
  | FamilyMemberHistoryFhirPatchBuilder
  | FlagFhirPatchBuilder
  | FormularyItemFhirPatchBuilder
  | GenomicStudyFhirPatchBuilder
  | GoalFhirPatchBuilder
  | GraphDefinitionFhirPatchBuilder
  | GroupFhirPatchBuilder
  | GuidanceResponseFhirPatchBuilder
  | HealthcareServiceFhirPatchBuilder
  | ImagingSelectionFhirPatchBuilder
  | ImagingStudyFhirPatchBuilder
  | ImmunizationFhirPatchBuilder
  | ImmunizationEvaluationFhirPatchBuilder
  | ImmunizationRecommendationFhirPatchBuilder
  | ImplementationGuideFhirPatchBuilder
  | IngredientFhirPatchBuilder
  | InsurancePlanFhirPatchBuilder
  | InventoryItemFhirPatchBuilder
  | InventoryReportFhirPatchBuilder
  | InvoiceFhirPatchBuilder
  | LibraryFhirPatchBuilder
  | LinkageFhirPatchBuilder
  | ListFhirPatchBuilder
  | LocationFhirPatchBuilder
  | ManufacturedItemDefinitionFhirPatchBuilder
  | MeasureFhirPatchBuilder
  | MeasureReportFhirPatchBuilder
  | MedicationFhirPatchBuilder
  | MedicationAdministrationFhirPatchBuilder
  | MedicationDispenseFhirPatchBuilder
  | MedicationKnowledgeFhirPatchBuilder
  | MedicationRequestFhirPatchBuilder
  | MedicationStatementFhirPatchBuilder
  | MedicinalProductDefinitionFhirPatchBuilder
  | MessageDefinitionFhirPatchBuilder
  | MessageHeaderFhirPatchBuilder
  | MetadataResourceFhirPatchBuilder
  | MolecularSequenceFhirPatchBuilder
  | NamingSystemFhirPatchBuilder
  | NutritionIntakeFhirPatchBuilder
  | NutritionOrderFhirPatchBuilder
  | NutritionProductFhirPatchBuilder
  | ObservationFhirPatchBuilder
  | ObservationDefinitionFhirPatchBuilder
  | OperationDefinitionFhirPatchBuilder
  | OperationOutcomeFhirPatchBuilder
  | OrganizationFhirPatchBuilder
  | OrganizationAffiliationFhirPatchBuilder
  | PackagedProductDefinitionFhirPatchBuilder
  | PatientFhirPatchBuilder
  | PaymentNoticeFhirPatchBuilder
  | PaymentReconciliationFhirPatchBuilder
  | PermissionFhirPatchBuilder
  | PersonFhirPatchBuilder
  | PlanDefinitionFhirPatchBuilder
  | PractitionerFhirPatchBuilder
  | PractitionerRoleFhirPatchBuilder
  | ProcedureFhirPatchBuilder
  | ProvenanceFhirPatchBuilder
  | QuestionnaireFhirPatchBuilder
  | QuestionnaireResponseFhirPatchBuilder
  | RegulatedAuthorizationFhirPatchBuilder
  | RelatedPersonFhirPatchBuilder
  | RequestOrchestrationFhirPatchBuilder
  | RequirementsFhirPatchBuilder
  | ResearchStudyFhirPatchBuilder
  | ResearchSubjectFhirPatchBuilder
  | RiskAssessmentFhirPatchBuilder
  | ScheduleFhirPatchBuilder
  | SearchParameterFhirPatchBuilder
  | ServiceRequestFhirPatchBuilder
  | SlotFhirPatchBuilder
  | SpecimenFhirPatchBuilder
  | SpecimenDefinitionFhirPatchBuilder
  | StructureDefinitionFhirPatchBuilder
  | StructureMapFhirPatchBuilder
  | SubscriptionFhirPatchBuilder
  | SubscriptionStatusFhirPatchBuilder
  | SubscriptionTopicFhirPatchBuilder
  | SubstanceFhirPatchBuilder
  | SubstanceDefinitionFhirPatchBuilder
  | SubstanceNucleicAcidFhirPatchBuilder
  | SubstancePolymerFhirPatchBuilder
  | SubstanceProteinFhirPatchBuilder
  | SubstanceReferenceInformationFhirPatchBuilder
  | SubstanceSourceMaterialFhirPatchBuilder
  | SupplyDeliveryFhirPatchBuilder
  | SupplyRequestFhirPatchBuilder
  | TaskFhirPatchBuilder
  | TerminologyCapabilitiesFhirPatchBuilder
  | TestPlanFhirPatchBuilder
  | TestReportFhirPatchBuilder
  | TestScriptFhirPatchBuilder
  | TransportFhirPatchBuilder
  | ValueSetFhirPatchBuilder
  | VerificationResultFhirPatchBuilder
  | VisionPrescriptionFhirPatchBuilder;

/**
 * Allow referencing a patch builder type from its string ResourceType representation.
 */
export type ExtractPatchBuilder<
  TDomainResourceType extends AnyDomainResourceType
> = Extract<AnyFhirPatchBuilder, { resourceType: TDomainResourceType }>;
